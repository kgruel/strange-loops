# Infrastructure vertex: folds disk and proc facts
name: infra

loops:
    disk:
        fold:
            mounts: by mount
        boundary: when disk

    proc:
        fold:
            count: latest
        boundary: when proc

emit: infra.health
