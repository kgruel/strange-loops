[project]
name = "experiments"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "wcwidth>=0.2",
    "kdl-py>=1.2.0",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
