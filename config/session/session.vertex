// Session — lifecycle tracking for delegated sessions
// One entry per delegate (by who), latest wins.
// Kyle emits session facts with who=kyle/<agent>, the fold tracks state.

name "session"
store "./data/session.db"

loops {
  session {
    fold { items "by" "who" }
  }

  boundary when="session" status="closed"
}
