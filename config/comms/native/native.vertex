// Native — loops-native channels, no external source
// Direct emit from agents, ticks, notifications.

name "native"
store "./data/native.db"

loops {
  message {
    fold {
      items "by" "message_id"
    }
  }
}
