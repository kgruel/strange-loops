// loops monorepo — workspace root
// Local resolution target: `loops read` with no vertex name finds this.
// Delegates to project.vertex for all fold state.

observers {
  kyle { }
  loops-claude { }
  meta-claude { }
}
