1. HIGH — [`apps/loops/src/loops/cli/views/read.py:48`](/Users/kaygee/Code/loops/apps/loops/src/loops/cli/views/read.py:48): Router mutual exclusion is bypassed on the facts route. `read V --facts --at head --as-of 30d` enters the stream branch before the `--at`/`--as-of` check at line 108; `--at` is silently discarded and the event-time query runs. The exclusion check must occur before route selection. The same ordering also drops `--at` when combined with `--facts --since/--id`.

2. HIGH — [`apps/loops/src/loops/cli/views/fold.py:389`](/Users/kaygee/Code/loops/apps/loops/src/loops/cli/views/fold.py:389): Lens-declared fetches may silently ignore the temporal selector. Signature filtering deliberately omits `at`/`as_of` when a fetch does not declare them, while the response still carries cursor metadata. Existing fetch lenses such as [`lenses/graph.py:45`](/Users/kaygee/Code/loops/apps/loops/src/loops/lenses/graph.py:45) accept neither selector. Thus `read V --lens graph --at …` can label a head-derived answer as witness-cursor-derived. Unsupported lens fetches should refuse, or their fetch APIs must honor the selector.

3. HIGH — [`apps/loops/src/loops/commands/fetch.py:202`](/Users/kaygee/Code/loops/apps/loops/src/loops/commands/fetch.py:202): `--refs` escapes the selected cursor. `_walk_refs` receives neither `at` nor `as_of`, and its recursive `fetch_fold` at line 267 therefore reads every referenced entity at head. A historical primary fold can consequently include future versions of referenced entities. Thread the same selector through every walk hop.

4. MEDIUM — [`libs/engine/src/engine/vertex_reader.py:1005`](/Users/kaygee/Code/loops/libs/engine/src/engine/vertex_reader.py:1005): Aggregate `as_of` cuts member facts correctly, but inherited member fold specifications are resolved at head. `_collect_source_specs` calls `load_declaration(vpath)` without `as_of` at lines 299 and 310. An aggregate without its own loops can therefore replay `ts <= T` facts under a member ontology introduced after T, violating equal-cursor event-time semantics. Current aggregate membership may remain the disclosed derogation; member ontology still needs the cutoff.

5. MEDIUM — [`libs/engine/src/engine/witness.py:237`](/Users/kaygee/Code/loops/libs/engine/src/engine/witness.py:237): A5’s resolved tick-floor identity is discarded and later reconstructed incorrectly. [`witness_address.py:205`](/Users/kaygee/Code/loops/apps/loops/src/loops/cli/witness_address.py:205) keeps only the selected tick’s `fact_cursor`; `_resolve_anchor` then chooses by cursor fact rowid, not by the requested wall-clock floor. If multiple ticks share that cursor, including a tick after the requested mark, the report can name an arbitrary/wrong tick. Additionally, `resolve_tick_floor` at lines 443–445 treats any nonempty cursor as usable without joining it to `facts`, so a dangling cursor produces an unknown-fact error instead of A5’s `--as-of` teaching. Preserve the selected tick in the resolution metadata and validate its cursor.

The focused cursor/diff suites passed: 88 tests. The remaining requested areas—early aggregate refusal, JSON cursor fields on both gate branches, cursor-free JSON compatibility, independent diff reconstructions, collect count-only degradation, inclusive single-store cutoffs, and typed common address errors—were clean on inspection. A concurrent uncommitted engine change appeared during the later full-suite run; it was excluded from this pinned-commit review.