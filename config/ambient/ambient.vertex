// Ambient — passive attention traces
// Safari browsing history, screen time, session replay

name "ambient"
store "./data/ambient.db"

// TODO: sources — safari.loop (query ~/Library/Safari/History.db)
// TODO: session boundary detection (research threads)
// TODO: replay tick (ordered tab list for re-opening)

discover "./**/*.vertex"
