# Minecraft stack container status
# SSH to minecraft VM, get docker compose ps output

source: ssh deploy@192.168.1.42 "cd /opt/minecraft && docker compose ps --format json"
kind: minecraft
observer: hlab
format: ndjson

parse:
  select Name, Service, State, Health, Status, ExitCode, Image, Ports, Project, CreatedAt, RunningFor
