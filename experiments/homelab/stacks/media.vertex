// Media stack aggregation
// Folds container status facts into stack health

name "media"

loops {
  containers {
    fold {
      services "by" "Service"
      updated "latest"
    }
    boundary when="media.container"
  }
}

emit "media.health"
