// Infrastructure stack aggregation
// Folds container status facts into stack health

name "infra"

loops {
  containers {
    fold {
      services "by" "Service"
      updated "latest"
    }
    boundary when="infra.container"
  }
}

emit "infra.health"
