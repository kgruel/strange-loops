{
 "briefs": [
  {
   "slice": "Slice 1 \u2014 typed Address(kind, key) dataclass: converge emit/read/match on one address value-object, fixing cross-kind aliasing, slash-qualified mislift, and bare-target validation skip",
   "thesis": "Address handling today is scattered across five ad-hoc string helpers in three files, with two DISAGREEING split implementations and three suffix-matchers, which produces three confirmed defects: (a) colon-qualified refs alias across kinds via suffix-only matching, (b) emit slash-splits a declared-edge value that read qualifies, so the same value lifts to two different edges, and (c) bare declared-edge values skip emit-time validation entirely while read still lifts them. This slice introduces one frozen `atoms.Address(kind, key)` value-object with two constructors \u2014 `Address.parse` (self-describing colon/slash/bare, for the ref corpus) and `Address.for_edge` (declaration-qualified, SHARED by read-lift and emit \u2014 the convergence point) \u2014 plus exact `(kind, key)` tuple matching that replaces every suffix-matcher. The declaration licenses bare keys: emit adopts read's qualify behavior for declared edge fields; read never adopts emit's slash-splitting.",
   "thesis_note": "binding constraint discovered during research: 493 legacy-slash refs (kind/key, e.g. decision/atoms/n-on-fold-item) live in the project store and MUST keep matching \u2014 see risks and current_behavior",
   "files": [
    {
     "path": "libs/atoms/src/atoms/address.py",
     "role": "NEW \u2014 the Address(kind, key) frozen dataclass: `parse(raw)` (colon-first, then first-slash, then bare with kind=''), `for_edge(value, target_kind)` (colon\u2192verbatim kind:key, else\u2192(target_kind, whole value)), `__str__` \u2192 canonical 'kind:key'. Must be @dataclass(frozen=True) and stdlib-only (two atoms architecture ratchets enforce this).",
     "est_loc_delta": "+50"
    },
    {
     "path": "libs/atoms/src/atoms/__init__.py",
     "role": "Export Address: add to __all__ (near 'Edge' at line 44) and to _LAZY_IMPORTS map (near line 96: `\"Address\": (\"atoms.address\", \"Address\")`).",
     "est_loc_delta": "+2"
    },
    {
     "path": "libs/engine/src/engine/vertex_reader.py",
     "role": "Dissolve `_normalize_edge_address` (1229-1242) into `Address.for_edge`; `_lift_edges` (1245-1267) calls `a = Address.for_edge(part, target_kind); if a: edges.append(Edge(predicate=field_name, address=str(a)))`. Read output stays BYTE-IDENTICAL (str(Address(person,acme))=='person:acme').",
     "est_loc_delta": "-6"
    },
    {
     "path": "apps/loops/src/loops/commands/resolve.py",
     "role": "Emit path. Replace `_split_addr`/`_is_addr_candidate` with Address; make resolution FIELD-TYPE-AWARE (declared-edge field \u2192 `Address.for_edge(addr, target_kind)`, bare licensed; ref/general field \u2192 `Address.parse(addr)`, separator required). Change `_extract_edge_fields` (378-400) to return `dict[str,str]` field\u2192target_kind so emit can qualify bare edge values. Fixes defects (b) and (c).",
     "est_loc_delta": "-15"
    },
    {
     "path": "apps/loops/src/loops/surface.py",
     "role": "Match path. Delete `_address_matches_key` (274-276); rewrite `_inbound_count` (342-372), `_inbound_predicates` (318-339), `_compute_inbound_edges` (279-300), `_compute_inbound_refs` (266-271) to match by exact `Address(kind,key)` equality + bare(kind='') key-fallback. `_inbound_count`/`_inbound_predicates` gain a `kind` param (call sites 581/601/614/634 pass section.kind / w.section_kind). Simplify `_candidate_addr_kind` (200-216) to `Address.parse(v).kind or None` behind the existing whitespace guard. Fixes defect (a).",
     "est_loc_delta": "-10"
    },
    {
     "path": "apps/loops/src/loops/commands/emit.py",
     "role": "Consumer of `_extract_edge_fields` at 833-837. `r.field in edge_fields` is unchanged by the dict return (membership tests keys), but the `else set()` fallback at 836 becomes `else {}`. Verify no other assumption of set-ness.",
     "est_loc_delta": "+1"
    },
    {
     "path": "libs/atoms/tests/test_address.py",
     "role": "NEW \u2014 unit tests for Address.parse (colon binds tighter than key slash; legacy slash\u2192(kind,key); bare\u2192(kind='', key); empty-half\u2192None), Address.for_edge (bare\u2192qualify, slash-key\u2192qualify NOT split, colon\u2192verbatim), __str__ round-trip, frozen.",
     "est_loc_delta": "+60"
    },
    {
     "path": "apps/loops/tests/test_fold_utils.py",
     "role": "Update `TestInboundCount`/`TestComputeInboundEdges` (149-221) to the exact-match contract: colon/slash-qualified refs match by (kind,key); bare by key; cross-kind colon no longer aliases. `test_with_refs` (156-158) and `test_namespaced_key_matches_both_forms` (160-174) need rewriting to the new kind-aware signature \u2014 legacy-slash MUST still match its own kind.",
     "est_loc_delta": "+15"
    },
    {
     "path": "apps/loops/tests/test_emit_receipt.py",
     "role": "`TestAddressSplit` (596-623) tests `_split_addr`/`_is_addr_candidate` directly (600-621). Retarget to Address.parse / the new candidate logic, or delete if those private fns are removed. Add a bare-declared-edge emit-receipt case (defect c: `stakeholder=acme` now produces 'inbound edge via stakeholder: person:acme').",
     "est_loc_delta": "+12"
    },
    {
     "path": "apps/loops/tests/test_typed_edges.py",
     "role": "`_extract_edge_fields` regression (299-332) must still pass with the dict return (per-kind scoping preserved). Add coverage: emit-side declared-edge value `design/foo` resolves to person:design/foo (defect b \u2014 agrees with read lift), not design:foo.",
     "est_loc_delta": "+10"
    }
   ],
   "current_behavior": "EMIT (apps/loops/src/loops/commands/resolve.py): `_resolve_entity_refs` scans payload; for `ref` and declared-edge fields it comma-splits (1010-1014), then `_is_addr_candidate(addr)` (1017) requires a `:` or `/` separator (827-840), then `_resolve_one(addr)` (955-990) calls `_split_addr` (806-824) which splits colon-first, else first-slash. `_extract_edge_fields` (378-400) returns only `set[str]` of field NAMES \u2014 the target kind is discarded. Consequence: a declared-edge value `stakeholder=design/foo` is slash-split to kind=design,key=foo (defect b); a bare `stakeholder=acme` fails `_is_addr_candidate` and is silently skipped \u2014 no validation, no receipt edge line (defect c). READ (libs/engine/src/engine/vertex_reader.py): `_lift_edges` (1245-1267) calls `_normalize_edge_address(part, target_kind)` (1229-1242) which keeps colon verbatim else qualifies the WHOLE value with target_kind \u2014 so `design/foo`\u2192`person:design/foo` (NOT split). Emit and read therefore disagree on the identical declared-edge value. MATCH (apps/loops/src/loops/surface.py): `_edge_corpus` (152-180) flattens item.refs (raw emitted strings \u2014 colon/legacy-slash/bare; confirmed 1587 colon, 493 legacy-slash, 73 bare in the live project store) and item.edges (already colon-normalized) into (address, predicate, source). `_address_matches_key(addr, key)` (274-276) matches `addr==key or addr.endswith('/'+key) or addr.endswith(':'+key)` \u2014 SUFFIX ONLY, kind-blind \u2014 so a corpus address `decision:design/foo` wrongly matches a `thread` row keyed `design/foo` (defect a). `_inbound_count` (342-372) duplicates the same three-form suffix scan inline over a flat `Counter[str]`, and has NO access to the target's kind (only item+key_field). `_compute_inbound_edges` (298) and `_inbound_predicates` (337) also route through `_address_matches_key`. Baseline: 186 loops tests + 22 engine edge tests pass; the aliasing behavior is currently LOCKED by test_fold_utils.py:156-174.",
   "planned_changes": [
    {
     "step": "1. Create atoms.Address",
     "description": "New libs/atoms/src/atoms/address.py: `@dataclass(frozen=True) class Address: kind: str; key: str`. `__str__` returns `f'{self.kind}:{self.key}'` when kind else the bare key. `@classmethod parse(cls, raw) -> Address|None`: strip; if empty\u2192None; if ':' in v \u2192 split first ':' (colon binds tighter than key slashes), reject empty half \u2192 Address(kind,key); elif '/' in v \u2192 split FIRST '/' \u2192 Address(kind, key) [PRESERVES the 493 legacy-slash refs]; else \u2192 Address('', v) [bare, kind unknown]. `@classmethod for_edge(cls, value, target_kind) -> Address|None`: strip; if empty\u2192None; if ':' in v \u2192 split first ':' \u2192 Address(kind,key); else \u2192 Address(target_kind, v) [declaration licenses the bare/slashed key \u2014 do NOT slash-split]. Export via __init__.py __all__ + _LAZY_IMPORTS.",
     "files": [
      "libs/atoms/src/atoms/address.py",
      "libs/atoms/src/atoms/__init__.py"
     ]
    },
    {
     "step": "2. Converge read-lift onto Address.for_edge",
     "description": "In vertex_reader `_lift_edges`, replace `addr = _normalize_edge_address(part, target_kind); if addr:` with `a = Address.for_edge(part, target_kind); if a: edges.append(Edge(predicate=field_name, address=str(a)))`. Delete `_normalize_edge_address`. Read output is byte-identical for well-formed values (verified: str(Address(person,acme))=='person:acme', and colon-verbatim cases round-trip). Engine edge/lift tests should stay green UNCHANGED \u2014 treat any diff as a regression to investigate, not to accept.",
     "files": [
      "libs/engine/src/engine/vertex_reader.py"
     ]
    },
    {
     "step": "3. Make _extract_edge_fields carry the target kind",
     "description": "Change return type to `dict[str,str]` mapping field\u2192target: `return {edge.field: edge.target for edge in getattr(loop_def, 'edges', ())}`. EdgeDecl has `.field` and `.target` (libs/lang/src/lang/loader.py:441). emit.py:836 `else set()`\u2192`else {}` (membership `r.field in edge_fields` is unchanged). resolve.py:1012 `field_name in local_edge_fields` is unchanged (dict key membership).",
     "files": [
      "apps/loops/src/loops/commands/resolve.py",
      "apps/loops/src/loops/commands/emit.py"
     ]
    },
    {
     "step": "4. Make emit resolution field-type-aware (fixes b + c)",
     "description": "In _resolve_entity_refs: rename local_edge_fields\u2192local_edge_targets (dict). For each (field_name, addr): if field_name is a declared edge field \u2192 `address = Address.for_edge(addr, local_edge_targets[field_name])` and treat ANY non-empty/non-whitespace value as a candidate (declaration licenses bare \u2014 drop the separator requirement for this path); else (ref/general) \u2192 require separator via Address.parse and keep the whitespace/prose guard. Refactor `_resolve_one(addr)` to accept the pre-built Address (drop its internal `_split_addr`). Use the same Address for the unresolved-pin block (replaces `_split_addr` at 1025). Net: `_split_addr` and `_is_addr_candidate` dissolve into Address.parse/for_edge + a small per-path candidate check.",
     "files": [
      "apps/loops/src/loops/commands/resolve.py"
     ]
    },
    {
     "step": "5. Replace suffix-matchers with exact Address matching (fixes a)",
     "description": "In surface.py: (i) `_compute_inbound_refs` builds `Counter[Address]` by `Address.parse`-ing every corpus address (bare\u2192Address('',key)). (ii) `_inbound_count(item, kind, key_field, inbound)` gains `kind`; returns `inbound[Address(kind,key)] + inbound[Address('', key)]` \u2014 O(1) exact lookup + bare fallback, replacing the three-form scan. (iii) `_inbound_predicates(item, kind, key_field, corpus)` gains `kind`; matches each corpus addr by `a=Address.parse(addr); (a.kind==kind and a.key==key) or (a.kind=='' and a.key==key)`. (iv) `_compute_inbound_edges` (already has section.kind) uses the same exact predicate; KEEP its output keyed by `_address(section.kind, key, id)` slash-form so the fetch.py graph consumer contract is unchanged. (v) delete `_address_matches_key`. (vi) `_candidate_addr_kind` \u2192 `Address.parse(v).kind or None` behind the existing empty/whitespace guard. Update call sites 581/601/614/634 to pass section.kind / w.section_kind.",
     "files": [
      "apps/loops/src/loops/surface.py"
     ]
    },
    {
     "step": "6. Update the locked tests + add convergence coverage",
     "description": "Rewrite the aliasing-locked tests (test_fold_utils.py:156-174) to the kind-aware exact-match contract; assert legacy-slash still matches its own kind AND that cross-kind colon no longer aliases. Add: bare declared-edge emit produces the receipt edge line (defect c); emit `stakeholder=design/foo` resolves to person:design/foo not design:foo (defect b). Retarget/remove the direct `_split_addr`/`_is_addr_candidate` tests (test_emit_receipt.py:596-623). New atoms test_address.py for the value object. Add a targeted anti-aliasing test: a vertex with both decision:X and thread:X, a ref to decision:X, assert only the decision row's \u2190count increments.",
     "files": [
      "libs/atoms/tests/test_address.py",
      "apps/loops/tests/test_fold_utils.py",
      "apps/loops/tests/test_surface.py",
      "apps/loops/tests/test_emit_receipt.py",
      "apps/loops/tests/test_typed_edges.py"
     ]
    }
   ],
   "test_plan": [
    "uv run --package atoms pytest libs/atoms/tests/test_address.py \u2014 new value-object unit tests (parse colon/slash/bare/empty; for_edge qualify-not-split; __str__ round-trip; frozen)",
    "uv run --package engine pytest libs/engine/tests -k 'edge or normalize or lift or refs' \u2014 MUST stay green with NO test edits (read-lift swap is byte-preserving); a required change signals a real regression",
    "uv run --package loops pytest apps/loops/tests/test_fold_utils.py apps/loops/tests/test_surface.py apps/loops/tests/test_typed_edges.py apps/loops/tests/test_emit_receipt.py",
    "New anti-aliasing regression: fixture with decision:design/foo and thread:design/foo both present + a ref=decision:design/foo \u2192 assert decision row \u2190count +1, thread row \u2190count unchanged (defect a)",
    "Legacy-slash preservation: ref=decision/atoms/n-on-fold-item against a decision row keyed atoms/n-on-fold-item \u2192 still counts (guards the 493 live refs)",
    "Emit convergence: `sl emit ... decision stakeholder=design/foo` then `sl read --refs` \u2192 edge address person:design/foo, matching read-lift; and bare `stakeholder=acme` -v \u2192 'inbound edge via stakeholder: person:acme' receipt line (defect c)",
    "uv run --package loops pytest apps/loops/tests -q (full loops suite incl. goldens) \u2014 inspect any golden \u2190N delta and confirm it is de-aliasing (counts DECREASE only where a cross-kind colon alias was over-counting) before regenerating with --update-goldens",
    "uv run --package engine pytest libs/engine/tests -q and uv run --package atoms pytest libs/atoms/tests -q (full)",
    "Re-run tests/test_architecture.py \u2014 confirm test_lib_dataclasses_frozen and test_atoms_stdlib_only accept Address, no DAG violation",
    "After CLI-code changes: `uv tool install . -e` then exercise real `sl emit`/`sl read --refs`/`sl read --lens graph` on the live project vertex per CLAUDE.md (installed-sl parity)"
   ],
   "acceptance_criteria": [
    "A single atoms.Address(kind, key) frozen dataclass is the only address parser/builder; `_split_addr`, `_is_addr_candidate`, `_normalize_edge_address`, and `_address_matches_key` are removed (dissolved into Address + per-path candidate checks) \u2014 residue swept, no dead helpers left",
    "Defect (a) fixed: a colon- or slash-qualified corpus address matches ONLY a row of its own kind; decision:X and thread:X no longer cross-alias in \u2190counts, \u2190predicate breakdowns, or --refs adjacency",
    "Defect (b) fixed: emit and read produce the SAME edge for a declared-edge value \u2014 `stakeholder=design/foo` \u2192 person:design/foo on both sides",
    "Defect (c) fixed: a bare declared-edge value (`stakeholder=acme`) is validated at emit time and produces the inbound-edge receipt line, matching the edge read already lifts",
    "Legacy-slash refs (kind/key) and bare refs continue to match exactly as before for their own kind \u2014 no regression to the 493 live slash-refs; read-lift output is byte-identical",
    "Emit adopts read's bare-key interpretation for declared edge fields; read does NOT slash-split declared-edge values (Address.for_edge qualifies, never splits) \u2014 the direction's asymmetry holds",
    "The fetch.py graph consumer contract is intact: `_compute_inbound_edges` still keys targets by the kind/key slash-form Row address",
    "Full atoms + engine + loops suites green; goldens regenerated only after the \u2190N diff is confirmed to be intended de-aliasing",
    "Address interface is compatible with future array-valued edge fields (a value is one Address; a set is a tuple of Address) but no array machinery is built (rider honored)"
   ],
   "risks": [
    "LEGACY-SLASH BLAST RADIUS (highest): the live project store holds 493 kind-qualified slash refs (decision/atoms/..., thread/..., rfc/...). Address.parse MUST slash-split the ref/corpus population (colon-first, then FIRST slash) or these silently stop counting. The direction's 'read must not adopt slash-splitting' applies ONLY to the declared-edge population (Address.for_edge), NOT to self-describing refs. Do not 'simplify' Address.parse to drop the slash branch.",
    "Two populations, two parse rules \u2014 this is deliberate, not duplication: refs are self-describing (parse), declared-edge values are declaration-qualified (for_edge). A reviewer may push to unify them; unifying would reintroduce defect (b). Document the boundary in Address docstrings.",
    "GOLDEN DRIFT: exact matching lowers \u2190N wherever cross-kind colon aliasing currently over-counts. Any golden over a vertex with cross-kind key collisions will shift. Inspect each delta and confirm it is de-aliasing before --update-goldens; a count that changes in the wrong direction (legacy-slash dropping) is a bug.",
    "`_inbound_count`/`_inbound_predicates` signature change (adding kind) touches 4 call sites (581/601/614/634) plus test imports (test_surface.py:37-38, test_fold_utils.py:20) \u2014 mechanical but easy to miss one and get a kind-less fallback that silently reverts to aliasing.",
    "`_salience` (surface.py:313-315) calls the old `_inbound_count` signature but appears DEAD (salience is materialized inline at 602/635; only imported by test_surface.py:38). Confirm dead and delete, or update its signature \u2014 do not leave a stale caller.",
    "Bare refs (kind='') remain key-only matches (attach to any kind bearing the key) \u2014 unavoidable and pre-existing (fetch.py:348 already refuses to walk bare refs), but confirm this does not surprise the salience goldens.",
    "Emit path is CLI code: per CLAUDE.md, `uv tool install . -e` and exercise the installed `sl` after changes \u2014 the uv-run form can mask install staleness."
   ],
   "dependencies": [
    "Coordinates with the sibling 'graph build-2' slice: this slice keeps `_compute_inbound_edges` output keyed by the kind/key slash-form Row.address (the fetch.py:1010-1090 graph builder consumes it). If graph build-2 reconciles Row.address (slash) vs edge.address (colon) node identity, land Address first so build-2 can consume Address instead of re-deriving splits \u2014 flag to whoever owns that slice.",
    "libs/lang EdgeDecl.field / EdgeDecl.target (loader.py:441, document.py:483) is the source of the field\u2192target_kind map _extract_edge_fields will now return \u2014 no lang change needed, just read .target.",
    "atoms architecture ratchets (tests/test_architecture.py): test_lib_dataclasses_frozen (Address must be frozen) and test_atoms_stdlib_only (no external imports). No DAG change (atoms stays leaf; engine\u2192atoms and apps\u2192atoms already allowed).",
    "No store migration: raw ref/edge strings in the DB are untouched; this is purely a read-time interpretation + emit-time resolution change (retroactive, like the existing edge-declaration lift)."
   ],
   "open_questions": [
    "Should Edge.address (atoms/fold_state.py:33) BECOME an Address, or stay a canonical 'kind:key' str with Address used only as the internal parse/build/match value-object? Research recommends STR-STAYS: edge.address is consumed as a string in lenses/fold.py:1315 and fetch.py:330 and across the graph builder + goldens, so typing the field ripples widely for no defect-fixing benefit \u2014 Address 'carried through lift/corpus/matching' is satisfied by using it at the boundaries and stringifying into the stable field. But the spec wording ('a typed Address carried through typed-edge lift') admits the stronger reading, so this is the one genuine fork for the arbiter: contained-value-object (recommended) vs typed-field (wider blast radius, more 'honest' typing).",
    "Is dropping legacy-slash-as-kind-qualified ever acceptable in exchange for a simpler single parse rule? Research says NO for this slice (493 live refs), but if a future slice migrates all slash refs to colon at rest (a store rewrite), Address.parse could shed its slash branch. Confirm the arbiter wants legacy-slash preserved indefinitely at read time vs scheduled for a one-time migration."
   ],
   "measures": [
    "LOC delta of address-handling code: capture `git diff --stat` scoped to the five touched source files; expect net-negative in resolve.py + surface.py + vertex_reader.py (five helpers dissolve into one ~50-LOC atoms module reused 3x)",
    "Slash-overload class elimination \u2014 count sites that string-split/suffix-match addresses BEFORE (verified this session): 2 disagreeing split impls (_split_addr colon-first vs _normalize_edge_address qualify) + 3 suffix-matchers (_address_matches_key, _inbound_count inline 363-369, _candidate_addr_kind 212-215) across 3 files \u2248 5 distinct helpers / ~8 split-or-suffix code sites. AFTER: 2 constructors on 1 Address class (parse, for_edge) in 1 file + exact tuple-equality matching (0 suffix-matchers). Report the before/after counts via `grep -n 'split(\\\":\\\"\\|split(\\\"/\\\"\\|endswith(f\\\":\\|endswith(f\\\"/'` over the three files.",
    "Convergence proof: number of distinct functions that turn a declared-edge value into (kind,key) \u2014 BEFORE 2 (emit _split_addr misuse + read _normalize_edge_address, disagreeing); AFTER 1 (Address.for_edge, shared).",
    "Test-surface simplification: net test-LOC and count of multi-form/aliasing assertions replaced by single exact-match assertions (test_fold_utils.py three-form tests \u2192 kind-aware exact); report tests deleted vs added."
   ]
  },
  {
   "acceptance_criteria": [
    "`sl store reindex <vertex>` exists, dispatches through `_run_store`, and is the ONLY code path that creates or writes `facts_fts`/`fts_state` anywhere in the codebase (grep for `CREATE VIRTUAL TABLE facts_fts` and `INSERT INTO facts_fts` \u2014 both must appear only inside the new reindex primitive).",
    "A read (`sl read <vertex> --match Q`, or a direct `vertex_search()` call) never opens a write-mode SQLite connection to the canonical store \u2014 verified by a regression test that captures the store .db's bytes before and after a search and asserts equality, both when facts_fts already exists AND when it has never been built.",
    "A newly-declared `search` field on a kind with pre-existing facts becomes fully searchable after one `sl store reindex` call, including facts emitted BEFORE the declaration existed (closes friction:fts-search-declaration-not-retroactive) \u2014 because reindex is a full drop+rebuild against the CURRENT declaration set, not an incremental watermark.",
    "A stale or missing FTS index never produces a silently-incomplete answer: `sl read <vertex> --match Q` for a kind whose index doesn't cover its newest facts falls back to the existing substring scan for that kind, and `Window`'s disclosure names the gap (distinct from the existing 'kind has no search declaration' disclosure).",
    "`sl store reindex <aggregate-vertex>` (a `combine` vertex) reindexes every child's own store \u2014 matching `_combined_search`'s existing per-child recursion \u2014 so the aggravator (one read against an aggregate mutating every child) has no analogue on the write side either: reindex is explicit per-child, never triggered by a read.",
    "All ~29 existing tests that call `vertex_search()`/`search()` without an explicit reindex step (libs/engine/tests/test_vertex_reader.py: TestVertexSearch, TestFTS5NestedFields, TestCombinedVertexSearch; libs/engine/tests/test_ontology_as_of.py: test_vertex_search_head_equals_now; apps/loops/tests/test_surface.py: the four `_search_vertex`-based tests; apps/loops/tests/test_integration.py: test_read_search) pass after inserting the new explicit reindex call \u2014 none silently pass by accident (i.e. none are secretly exercising the substring fallback while claiming to test FTS).",
    "`test_store_completion.py`'s `TestStoreSubcommandsParity` (source-order regex against `STORE_SUBCOMMANDS`) passes unmodified against the new 8-subcommand dispatch chain \u2014 this is the existing ratchet that would fail loudly if reindex were wired into `_run_store` without also appearing in `cli/store_args.STORE_SUBCOMMANDS` at the same relative position.",
    "design:fts-confirm-symmetry-is-phantom's property (a just-emitted fact IS found by the very next `--match`) is explicitly re-derived, not silently dropped: post-fix it holds via the substring-fallback route for the just-emitted fact's kind (flagged stale/missing-index in Window), not via FTS auto-catch-up \u2014 this must be a named test, not just an emergent side effect.",
    "`uv run --package engine pytest libs/engine/tests` and `uv run --package loops pytest apps/loops/tests` both pass."
   ],
   "current_behavior": "Every FTS content search writes to the canonical store before it reads. apps/loops/src/loops/surface.py:798 (`facts = vertex_search(vertex_path, query)` inside `search()`, def at 761-823) calls libs/engine/src/engine/vertex_reader.py's `vertex_search()` (def 1651-1726), which unconditionally calls `_ensure_fts(store_path, search_fields)` at line 1718 BEFORE running the actual FTS5 query. `_ensure_fts` (def 1585-1648) opens a plain read-write `sqlite3.connect(str(store_path))` (line 1593, no `PRAGMA query_only`), `CREATE TABLE IF NOT EXISTS`s `facts_fts`/`fts_state` (1597-1607), scans every fact with `rowid > fts_state.last_rowid` (1615-1619), inserts FTS rows for kinds with a declared `search` field, advances `last_rowid` to the last scanned rowid REGARDLESS of whether that kind was searchable (1622-1628, deliberately, per the docstring, so trailing non-searchable rows like `_decl.*` don't get rescanned forever), and `conn.commit()`s (1646) if anything advanced. This is the confirmed mutation (184320\u2192282624 bytes on a nominal read). AFTER this write, `vertex_search` opens a SEPARATE, genuinely-read-only `StoreReader` (libs/engine/src/engine/store_reader.py:13-31; `PRAGMA query_only=ON` at line 31) and calls `reader.search_facts(...)` (store_reader.py:539-583) \u2014 this second half is already read-only today; only `_ensure_fts`'s connection writes. For aggregation: `_combined_search` (vertex_reader.py, def 706-758) is entered when `ast.combine is not None` (checked inside `vertex_search` at line 1696-1701); it loops over `ast.combine` entries (742-748) and calls `vertex_search(vpath, query, ...)` RECURSIVELY per child at line 749 \u2014 each recursive call re-triggers `_ensure_fts` against that child's OWN store.db, confirming the aggravator (one aggregate read mutates every child). Retroactivity bug: because `_ensure_fts` advances `last_rowid` past EVERY scanned row including non-searchable ones, a fact whose kind had no `search` declaration at scan time is permanently skipped even if `search` is declared on that kind later \u2014 the watermark never revisits it (fts-search-declaration-not-retroactive). Coverage-gap disclosure exists today ONLY for the 'never declared' case: surface.py's `_indexed_kinds()` (744-758) parses the vertex file's `loops{}` block for kinds with a `search` field; `search()` computes `unindexed = present - indexed` (788) and substring-scans those rows (806-812); `Window.unindexed` (surface.py:106-119, field at 119) carries this tuple through to `to_dict`'s `_window_to_dict` (1005-1014, line 1013) and to the TTY footer in apps/loops/src/loops/lenses/fold.py's `_render_search` (559-618, footer at 610-616, MINIMAL-zoom count at 576-577). There is NO staleness axis today \u2014 a kind that IS declared searchable but whose newest facts postdate the last `_ensure_fts` scan is treated as fully covered (the write-on-read silently 'fixes' this on every call, which is exactly the mutation being removed). `vertex_search` has exactly one production caller (surface.py:798) plus its own recursive self-call (vertex_reader.py:749) \u2014 no other module imports it (verified by repo-wide grep), so its contract can change with a small, fully-enumerable blast radius. `sl store` has no `reindex` subcommand today; apps/loops/src/loops/commands/store.py's `_run_store` (1342-1445) dispatches on seven subcommands (`verify`/`rebirth`/`reanchor`/`absorb`/`adopt`/`ticks`/`stats`, if/elif chain at 1355-1368) and apps/loops/src/loops/cli/store_args.py's `STORE_SUBCOMMANDS` tuple (line ~41-42) lists exactly those seven; apps/loops/tests/test_store_completion.py's `TestStoreSubcommandsParity.test_matches_run_store_dispatch_chain` (lines 31-37) regex-extracts the dispatch chain from `_run_store`'s source IN ORDER and asserts it equals the tuple verbatim \u2014 a ratchet that will fail loudly (correctly) if reindex is wired into one without the other, or added out of relative order between them. Manually rebuilding the index today requires hand-dropping `facts_fts`/`fts_state` via raw `sqlite3` (no CLI surface), which is exactly what the friction fact 'fts-search-declaration-not-retroactive' hit live on 2026-07-11.",
   "dependencies": [
    "None from other 0.9.0 slices \u2014 this is self-contained within libs/engine + apps/loops/surface+lenses+commands+cli and their tests. It does not touch atoms, lang, custody, sign, or libs/store.",
    "The `search-read-mutates-canonical-store` and `fts-search-declaration-not-retroactive` frictions and the `fts-confirm-symmetry-is-phantom`/`fts-match-limit-100-silent-cap` facts are already read (quoted verbatim above) \u2014 no further loops-store reads are required before starting.",
    "Soft dependency, not blocking: whichever slice touches Slice 1 (typed-address) should not be mid-flight in surface.py's `search()`/`Window` at the same time \u2014 both slices edit apps/loops/src/loops/surface.py, so sequence or rebase to avoid a merge conflict on the `search()` function and the `Window` dataclass."
   ],
   "files": [
    {
     "path": "libs/engine/src/engine/vertex_reader.py",
     "role": "Remove `_ensure_fts`'s call from the read path; add `vertex_reindex()` (the sole writer, full drop+rebuild) and a read-only `vertex_search_coverage()` staleness probe, both combine/discover-aware (mirror `_combined_search`'s recursion at 706-758). `vertex_search()` (1651-1726) loses the `_ensure_fts(store_path, search_fields)` call at line 1718 but is otherwise unchanged (its `StoreReader`-based query at 1720-1726 was already read-only).",
     "est_loc_delta": "+150/-70"
    },
    {
     "path": "libs/engine/src/engine/__init__.py",
     "role": "Export `vertex_reindex` and `vertex_search_coverage` via the lazy-import map (mirror the `vertex_search` entry at line 218) and add both names to `__all__` (near line 90).",
     "est_loc_delta": "+6/-0"
    },
    {
     "path": "apps/loops/src/loops/surface.py",
     "role": "`search()` (761-823): call the new coverage probe alongside `_indexed_kinds()`; route stale/missing-index kinds through the existing substring-fallback branch (806-812) instead of the FTS branch (794-801); fix or narrow the `except Exception: facts = []` swallow at 799-800 so it is a documented defensive backstop, not the mechanism that hides a missing-table case. `Window` (105-119): add a new field for index-staleness (recommended name: `stale`, keeping `unindexed` meaning unchanged \u2014 see planned_changes for rationale). `_window_to_dict` (1005-1014): serialize the new field.",
     "est_loc_delta": "+35/-10"
    },
    {
     "path": "apps/loops/src/loops/lenses/fold.py",
     "role": "`_render_search` (559-618): extend the MINIMAL one-liner (576-577) and the footer (610-616) to disclose stale-index kinds distinctly from never-indexed kinds (e.g. '(K index stale \u2014 run `sl store reindex`)' vs '(K not indexed)').",
     "est_loc_delta": "+15/-5"
    },
    {
     "path": "apps/loops/src/loops/commands/store.py",
     "role": "Add `_run_reindex(argv, *, vertex_path=None) -> int`, mirroring `_run_reanchor`'s shape (488-561: require a `.vertex` target, refuse `.db`/name-only with an actionable message, require the store to exist, call the engine primitive, render a one-line receipt or `--json`). Wire it into `_run_store`'s dispatch chain (1342-1445, elif block at 1355-1368) \u2014 append after the `stats` branch to keep `STORE_SUBCOMMANDS` order-parity trivial.",
     "est_loc_delta": "+90/-2"
    },
    {
     "path": "apps/loops/src/loops/cli/store_args.py",
     "role": "Append `\"reindex\"` to `STORE_SUBCOMMANDS` (line ~41-42) in the SAME relative position as the new `_run_store` elif branch (parity test enforces exact source order), update the `subcommand` help string.",
     "est_loc_delta": "+2/-1"
    },
    {
     "path": "libs/engine/tests/test_vertex_reader.py",
     "role": "TestVertexSearch (433-744, ~14 tests), TestFTS5NestedFields (807-886, 4 tests), TestCombinedVertexSearch (1420-1528, 6 tests): insert an explicit `vertex_reindex(vpath)` call before each `vertex_search(...)` assertion. Rewrite `test_incremental_catchup` (626-655) to assert the NEW contract (a fact added after the last reindex is NOT found until reindex runs again \u2014 rename to reflect that). `test_fts_watermark_advances_past_trailing_nonsearchable` (657-691): drive the watermark check through `vertex_reindex` instead of `vertex_search`. Add: a byte-identity regression test for `vertex_search` (with and without a pre-existing index) closing friction:search-read-mutates-canonical-store; a retroactive-indexing test closing friction:fts-search-declaration-not-retroactive (facts pre-dating a `search` declaration are found after one reindex); a per-child aggregate reindex test mirroring TestCombinedVertexSearch's `_setup_search_combine_env` fixture.",
     "est_loc_delta": "+140/-25"
    },
    {
     "path": "libs/engine/tests/test_ontology_as_of.py",
     "role": "`test_vertex_search_head_equals_now` (215-224): insert `vertex_reindex(vpath)` before both `vertex_search` calls.",
     "est_loc_delta": "+2/-0"
    },
    {
     "path": "apps/loops/tests/test_surface.py",
     "role": "`_search_vertex` fixture (493-521): call the new reindex primitive right after the two `emit(...)` calls so the four downstream tests (524-570) genuinely exercise FTS again instead of silently degrading to the substring fallback. Add: an unreindexed-vertex test asserting substring fallback + staleness disclosure (not a crash, not a silent empty result) \u2014 the direct test for FIX SHAPE point 4.",
     "est_loc_delta": "+35/-3"
    },
    {
     "path": "apps/loops/tests/test_integration.py",
     "role": "`test_read_search` (115-122) and `test_search_empty_vertex` (415-423): insert the explicit reindex step where facts exist before the search assertion (the empty-vertex case needs no change \u2014 it returns `[]` before ever touching search fields).",
     "est_loc_delta": "+4/-0"
    },
    {
     "path": "apps/loops/tests/test_store_command.py",
     "role": "Add a `TestStoreReindex` class mirroring `TestStoreVerify`/`TestStoreStats` (95-145, 311-364): happy path (fresh store \u2192 reindex \u2192 facts_fts exists and is queryable), `.db`-target refusal (mirror `test_reanchor_db_target_is_clean_error`, 383-393), no-facts-yet refusal, `--json` receipt shape, and an aggregate-vertex reindex that touches every child's own store.db.",
     "est_loc_delta": "+80/-0"
    },
    {
     "path": "docs/CLI-CHEATSHEET.md",
     "role": "Update the FTS caveat (lines 78-81: 'FTS content search (--match) is a head-built index...') to describe the new explicit-reindex contract and the stale-index disclosure; mention `sl store reindex` near the `--match`/`--grep` grammar block (129-130).",
     "est_loc_delta": "+8/-3"
    }
   ],
   "measures": [
    "Byte-for-byte (or at minimum: row-count / table-existence) identity of the store .db before and after a `--match` read, in both the pre-existing-index and never-indexed cases \u2014 the direct regression measure for the 184320\u2192282624-byte mutation.",
    "Count of existing tests requiring an explicit-reindex insertion (target: the ~29 enumerated above, none silently mis-passing post-fix).",
    "STORE_SUBCOMMANDS / `_run_store` dispatch-chain parity (existing ratchet in test_store_completion.py) stays green with zero modification to the test itself."
   ],
   "open_questions": [
    "Window disclosure shape for the new staleness axis: reuse the existing `unindexed` tuple (folding 'never declared' and 'declared but stale' into one undifferentiated signal \u2014 the more literal reading of the friction fact's 'Window.unindexed disclosure extended to name index-staleness'), or add a new distinct field (recommended in this brief as `Window.stale`, keeping `unindexed` semantics unchanged) so the two reasons stay honestly distinguishable in both the TTY footer and the `--json` contract. This is a public-surface (JSON contract) decision, not pure implementation detail, and the friction fact's wording is genuinely ambiguous between the two readings \u2014 recommend the separate-field shape (consistent with Window's existing one-field-per-distinct-signal convention: `limited_by`, `granularity`, `unindexed` are already separate axes) but flagging since it changes what `--json` consumers see."
   ],
   "planned_changes": [
    {
     "step": "1. Engine: add `vertex_reindex(vertex_path: Path) -> dict`",
     "description": "New function in libs/engine/src/engine/vertex_reader.py, placed near `_ensure_fts`'s old location. For a leaf (single-store) vertex: resolve `store_path` via the same logic `vertex_search` uses (1703-1711); if the store doesn't exist, return a no-op receipt (nothing to index yet \u2014 do NOT create the .db). `collect_search_fields(ast, base_dir)`; if empty, return a no-op receipt without touching the DB (nothing declared searchable). Otherwise open a plain read-write `sqlite3.connect`, `DROP TABLE IF EXISTS facts_fts; DROP TABLE IF EXISTS fts_state;` then recreate both with the SAME schema `_ensure_fts` used (1596-1607), scan ALL facts ordered by rowid (no `WHERE rowid > ?` \u2014 this is what fixes the retroactivity friction: every reindex re-evaluates every fact against the CURRENT declaration set), insert FTS rows for kinds present in `search_fields`, set `fts_state.last_rowid` to the max scanned rowid, commit, close. Return a receipt dict (vertex name, kinds indexed, facts scanned/indexed, last_rowid). For `ast.combine is not None`: recurse into each child exactly like `_combined_search` (706-758) does for search \u2014 resolve each child's vertex path via `lang.resolve_vertex`, call `vertex_reindex` on it, collect per-child receipts. For `ast.discover is not None`: match `_combined_search`'s existing gap (732-740, 'discover is not yet supported for search') \u2014 a documented no-op, not a regression to fix here.",
     "files": [
      "libs/engine/src/engine/vertex_reader.py"
     ]
    },
    {
     "step": "2. Engine: remove the write from `vertex_search()`, add a read-only staleness probe",
     "description": "Delete the `_ensure_fts(store_path, search_fields)` call at line 1718 (and the `_ensure_fts` function itself, its logic having moved into `vertex_reindex`). Add `vertex_search_coverage(vertex_path: Path) -> FtsCoverage` (a small frozen dataclass or tuple: `missing: bool`, `stale_kinds: frozenset[str]`) \u2014 open a `StoreReader` (already read-only), check whether `facts_fts`/`fts_state` exist (`sqlite_master` lookup); if absent, `missing=True` and skip further probing. If present, read `fts_state.last_rowid`, then `SELECT DISTINCT kind FROM facts WHERE rowid > ? AND kind IN (declared searchable kinds)` to find `stale_kinds` \u2014 kinds with facts added since the last reindex. Recurse through `ast.combine`/`ast.discover` exactly like `_combined_search`/`vertex_reindex`, OR-ing `missing` and unioning `stale_kinds` across children (a query for text unique to one stale child must not be silently missed). `StoreReader.search_facts` (store_reader.py:539) will raise `sqlite3.OperationalError` if `facts_fts` doesn't exist \u2014 `vertex_search` itself does not need to guard this internally (callers use the coverage probe FIRST to decide whether to call it at all); document this contract clearly in `vertex_search`'s docstring (1662-1687) so it isn't rediscovered as a crash later. Export both new functions via `engine/__init__.py`'s lazy map + `__all__`.",
     "files": [
      "libs/engine/src/engine/vertex_reader.py",
      "libs/engine/src/engine/__init__.py"
     ]
    },
    {
     "step": "3. Surface: consult the coverage probe, route stale kinds through the existing substring fallback wholesale",
     "description": "In `search()` (surface.py 761-823): alongside the existing `_indexed_kinds(vertex_path)` call, call `engine.vertex_search_coverage(vertex_path)` (guard `vertex_path is not None`, same as today's guard at 794). Compute `trustworthy = indexed - coverage.stale_kinds` if not `coverage.missing`, else `trustworthy = set()`. `fts_kinds = present & trustworthy` (only call `vertex_search` for kinds proven fresh \u2014 this also means `vertex_search`'s own FTS query is never issued against a stale/missing index, so its exception-swallow at line 799-800 stops being load-bearing for this case). The substring branch's `unindexed` set (788, 806-812) grows to `(present - indexed) | (present & indexed & (coverage.stale_kinds if not coverage.missing else indexed))` \u2014 i.e. a WHOLESALE substring scan of the stale kind, not a partial merge of trusted-FTS-results-up-to-the-watermark plus substring-for-the-gap-only. Rationale for wholesale over partial: the substring fallback already operates on already-folded `surface.rows` (not raw per-fact rowid-stamped events \u2014 see the existing docstring at 770-773), so there is no clean way to isolate 'just the gap' at this layer without inventing new merge/dedup machinery; reusing the EXISTING fallback path verbatim (per the friction fact's own wording) for the whole kind is simpler, avoids double-counting, and is still fully honest since nothing is silently dropped. Resolve the open_questions item (Window.stale vs. reusing Window.unindexed) and thread the chosen field through `Window` (105-119), `_window_to_dict` (1005-1014), and `_render_search` (lenses/fold.py 559-618).",
     "files": [
      "apps/loops/src/loops/surface.py",
      "apps/loops/src/loops/lenses/fold.py"
     ]
    },
    {
     "step": "4. CLI: add `sl store reindex`",
     "description": "Add `_run_reindex(argv, *, vertex_path=None) -> int` to apps/loops/src/loops/commands/store.py, mirroring `_run_reanchor` (488-561) closely: argparse with an optional `file` positional (vertex name/path) when `vertex_path is None`, `--json`. Resolve via `_resolve_target`; refuse a `.db`-suffixed target with an actionable message ('reindex requires a .vertex target \u2014 search field declarations live in the vertex file, not the raw database'), same pattern as reanchor's guard (512-516) and absorb's (703-707). Require the store to exist (mirror reanchor's `db_path.exists()` check, 518-519) UNLESS the target is a combine/discover aggregate (which has no own store \u2014 delegate straight to `engine.vertex_reindex`, which handles the recursion). Call `engine.vertex_reindex(target_path)`, render either the JSON receipt or a one-line painted summary (kinds indexed, facts scanned, receipt/no-op distinction) in the style of reanchor's rendering block (550-560). Wire into `_run_store`'s dispatch (1342-1445): add `if argv and argv[0] == \"reindex\": return _run_reindex(argv[1:], vertex_path=vertex_path)` as the LAST branch (after `stats`, before the base-inspect fallthrough at 1370) to minimize churn on the existing chain. In apps/loops/src/loops/cli/store_args.py, append `\"reindex\"` to `STORE_SUBCOMMANDS` at the matching final position and extend the subcommand help string \u2014 the two must move together or `TestStoreSubcommandsParity` (test_store_completion.py:31-37) fails by design.",
     "files": [
      "apps/loops/src/loops/commands/store.py",
      "apps/loops/src/loops/cli/store_args.py"
     ]
    },
    {
     "step": "5. Test migration: make every existing FTS test explicit about reindexing",
     "description": "Mechanical but non-optional: every existing test that calls `vertex_search()` (directly or via `surface.search()`) without ever calling the new reindex primitive currently relies on the write-on-read behavior being removed. Walk libs/engine/tests/test_vertex_reader.py's TestVertexSearch (433-744), TestFTS5NestedFields (807-886), TestCombinedVertexSearch (1420-1528); libs/engine/tests/test_ontology_as_of.py's test_vertex_search_head_equals_now (215-224); apps/loops/tests/test_surface.py's `_search_vertex`-fixture tests (493-570); apps/loops/tests/test_integration.py's test_read_search (115-122). Insert `vertex_reindex(vpath)` (or the equivalent through the fixture) immediately before the first `vertex_search`/`search()` call each test makes. Rewrite `test_incremental_catchup` (626-655) to assert the NEW behavior (a fact added after the last reindex is invisible to a bare `vertex_search` until reindex runs again) rather than deleting it outright \u2014 this is the direct regression test for 'reject write-side reindex' from design:fts-confirm-symmetry-is-phantom.",
     "files": [
      "libs/engine/tests/test_vertex_reader.py",
      "libs/engine/tests/test_ontology_as_of.py",
      "apps/loops/tests/test_surface.py",
      "apps/loops/tests/test_integration.py"
     ]
    },
    {
     "step": "6. New tests: the properties this fix is actually FOR",
     "description": "Add tests that would fail on the OLD code and pass on the new: (a) byte-identity of the store .db across a `--match`/`vertex_search` call, both with a pre-built index and with none at all (closes friction:search-read-mutates-canonical-store \u2014 the literal 184320\u2192282624-byte regression); (b) retroactive indexing \u2014 emit facts under a kind with NO `search` declaration, declare `search` on that kind (rewrite the vertex file or point a second vertex file at the same store), run `vertex_reindex`, assert ALL pre-declaration facts are now found (closes friction:fts-search-declaration-not-retroactive); (c) stale-index disclosure \u2014 emit, reindex, emit again WITHOUT reindexing, `search()` and assert the new fact is found via substring fallback (not silently missing) AND the staleness is named in `Window` (closes the 'no silent wrong answers' requirement and re-derives design:fts-confirm-symmetry-is-phantom's emit-then-read property under the new mechanism); (d) `sl store reindex` on an aggregate vertex touches every child's own store.db (closes the aggravator on the write side, symmetric with the read-side fix); (e) CLI-level `TestStoreReindex` class in apps/loops/tests/test_store_command.py covering the reindex verb itself (happy path, `.db`-target refusal, no-facts-yet, `--json` shape) per the existing `TestStoreVerify`/`TestStoreStats` template.",
     "files": [
      "libs/engine/tests/test_vertex_reader.py",
      "apps/loops/tests/test_surface.py",
      "apps/loops/tests/test_store_command.py"
     ]
    }
   ],
   "risks": [
    "The ~29-test migration (step 5) is mechanical but easy to under-count or half-do; a test that silently keeps passing post-fix without ever calling reindex would mean it degraded to testing the substring fallback while its docstring/name still claims to test FTS (exactly what would have happened to `test_search_fts_finds_bodies_in_indexed_kind` in apps/loops/tests/test_surface.py if the fixture weren't fixed) \u2014 a false-green that hides a real coverage gap. Mitigate by grepping for every `vertex_search(` and `search(project(` call site after the change and confirming each either follows a reindex call or is deliberately testing the NO-reindex/stale case.",
    "The `except Exception: facts = []` at surface.py:799-800 currently masks ANY error from `vertex_search`, including a hypothetical bug in the new coverage-probe-gating logic (e.g. a kind incorrectly judged 'trustworthy' when the table is actually missing) \u2014 this could silently degrade to '0 results' instead of surfacing a real defect. Recommend narrowing the except or adding an explicit assertion/test that this branch is never hit in the fixed happy path.",
    "Full drop+rebuild on `sl store reindex` is O(all facts) every time, unlike the old incremental catch-up. For this repo's current store sizes this is a non-issue (thousands of facts, sub-second), but it's a deliberate trade \u2014 worth a one-line comment at the call site explaining why full-rebuild was chosen over incremental-with-declaration-diffing (simplicity + it's what closes the retroactivity bug in the same primitive).",
    "`vertex_search_coverage`'s combine/discover recursion must stay in lockstep with `_combined_search`'s and `vertex_reindex`'s recursion (three places now walk `ast.combine`/`ast.discover` similarly) \u2014 a future change to one (e.g. discover support landing) that isn't mirrored in the other two would silently reintroduce a coverage gap. Consider factoring the child-resolution loop (vertex_reader.py 742-748) into a shared helper if it doesn't already read cleanly duplicated three ways.",
    "SQLite byte-identity assertions (step 6a) can be flaky if WAL-mode checkpointing or page-cache effects touch the file even with zero logical writes; if `SqliteStore`/`StoreReader` use WAL mode, prefer asserting on `facts_fts`/`fts_state` table absence + row counts rather than raw file bytes, or explicitly checkpoint before comparing."
   ],
   "slice": "FTS read-purity \u2014 sl store reindex as the sole index writer, read-only vertex_search, staleness disclosure",
   "test_plan": [
    "uv run --package engine pytest libs/engine/tests/test_vertex_reader.py -k 'Search or Fts or FTS or Combined' -v \u2014 the migrated + new engine-level tests.",
    "uv run --package engine pytest libs/engine/tests/test_ontology_as_of.py -v",
    "uv run --package engine pytest libs/engine/tests \u2014 full engine suite (nothing outside the FTS surface should be touched, but run it whole to catch DAG/import regressions from the __init__.py export additions).",
    "uv run --package loops pytest apps/loops/tests/test_surface.py -k 'search or Search or unindexed or fts' -v",
    "uv run --package loops pytest apps/loops/tests/test_store_command.py -k 'Reindex' -v \u2014 the new CLI-level tests.",
    "uv run --package loops pytest apps/loops/tests/test_store_completion.py -v \u2014 MUST pass unmodified; if it fails, STORE_SUBCOMMANDS and _run_store's dispatch chain drifted out of lockstep.",
    "uv run --package loops pytest apps/loops/tests \u2014 full app suite, including a golden-snapshot pass (apps/loops/tests/golden) to catch any incidental --match rendering drift.",
    "Manual/exercise via the installed CLI (per this repo's CLAUDE.md \u2014 after `uv tool install . -e`): in a scratch vertex, emit a fact, run `sl read <v> --match <word>` and confirm 0 results + a 'not indexed'/'stale' disclosure (not silently 0-with-no-explanation); run `sl store reindex <v>`; re-run the same `--match` and confirm the fact is now found; `xxd`/`stat` the store .db before and after the `--match` re-run to confirm zero byte change.",
    "Manual: run `sl store reindex <v>` twice in a row (idempotence check) and confirm identical results/no errors on the drop+recreate.",
    "Manual: against the config-level `project` combine aggregate (~/.config/loops/project/project.vertex), run `sl read project --match <word>` before touching any child's reindex state, and confirm no child store.db's bytes change (the direct end-to-end check for the aggravator)."
   ],
   "thesis": "Make `sl store reindex <vertex>` the sole writer of the FTS5 derived index (facts_fts/fts_state), and make every read path (vertex_search, and its aggregation recursion through combine/discover children) strictly read-only. A read today silently mutates the canonical SQLite store on first `--match` (confirmed 184320\u2192282624 bytes) and, for aggregates, mutates every child store \u2014 a nominal query corrupting the very artifact it's supposed to be inspecting. The fix also incidentally closes the retroactivity friction (a newly-declared `search` field never backfills old facts) for free, because the new reindex primitive is a full drop+rebuild against the CURRENT declaration set rather than an incremental append-only watermark. The one thing this fix must not silently break is the property design:fts-confirm-symmetry-is-phantom established (a just-emitted fact is found by the very next --match) \u2014 it currently holds because the read writes; post-fix it must be re-derived through the existing substring-fallback path with an explicit staleness disclosure, not quietly dropped."
  },
  {
   "slice": "Slice 3 \u2014 graph build-2: predicate-aware graph projection (fetch_graph key-honoring, --edge predicate selection, chain predicate retention, SCC/cycle census, dangling/orphan partition)",
   "thesis": "The graph lens (`--lens graph`) is a composition lens whose own `fetch(vertex_path, kind, observer)` silently drops `--key` and conflates every unresolved edge into one `dangling` counter. This slice widens `fetch_graph` to honor `--key` (honor-or-refuse, never silent-drop), adds an `--edge` predicate selector, retains edge-predicate labels through the chain DFS (they are already carried in the corpus and thrown away only at the walk boundary), reports an SCC/cycle census in the returned dict (forward-required once batch-emit makes cycles legal), and partitions today's single `dangling` count into distinct, honest categories (genuine-dangling vs filter-excluded, plus the keyless disclosure). It is a projection-and-render change confined to `commands/fetch.py`, `lenses/graph.py`, the `--edge` arg plumbing in `cli/`, and the graph tests/goldens; domain composition (capability/flow/stage views) is explicitly out of scope.",
   "thesis_notes": "n/a",
   "files": [
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/commands/fetch.py",
     "role": "PRIMARY \u2014 fetch_graph (def at line 966) + _longest_chains (848) + _top_chains (933). Widen signature to accept key/edge; add global-node-set fetch for (e) classification; retain predicates in DFS; add SCC census; split dangling counters in the returned dict (1090-1103).",
     "est_loc_delta": "+120 / -25"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/lenses/graph.py",
     "role": "PRIMARY \u2014 graph lens. Widen module-level fetch (45-49) to accept key/edge and forward. Extend graph_view (95) + _chain_line (74-92) to render per-hop predicate labels, the new dangling/filter-excluded/keyless breakdown on the counts line (52-60, 202-207, 306-313), and the cycle census section. fold_view re-export (329) unchanged.",
     "est_loc_delta": "+80 / -20"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/read_args.py",
     "role": "SECONDARY \u2014 add `--edge` argument (after --key, ~line 88) with a predicate completer stub. Declaration-only; parsed by the fold view.",
     "est_loc_delta": "+10"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/views/fold.py",
     "role": "SECONDARY \u2014 thread args.edge into _build_fold_fetch (391-438); pass edge= ONLY on the call_lens_fetch branch (418-428), never to fetch_fold. Optionally refuse `--edge` when the resolved lens fetch does not accept it, reusing the _lens_fetch_accepts_cursor idiom (367-388).",
     "est_loc_delta": "+25"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/completers.py",
     "role": "OPTIONAL \u2014 a complete_edge completer (predicate names from the vertex's census/declared edge fields), mirroring complete_key. Skip if deferring completion.",
     "est_loc_delta": "+15"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/tests/test_graph.py",
     "role": "TESTS \u2014 extend TestFetch/TestTraversal/TestZooms with key-honoring, --edge filtering, chain predicate retention, SCC census, and dangling/filter-excluded/keyless partition cases. 37 tests pass at baseline.",
     "est_loc_delta": "+180"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/tests/golden/goldens/tests.golden.test_grammar_parity/",
     "role": "GOLDENS \u2014 the 8 [*-graph] snapshot dirs change (chains line gains predicate labels; counts line gains the new breakdown). Regenerate with --update-goldens after impl.",
     "est_loc_delta": "regen"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/surface.py",
     "role": "REFERENCE (slice-1 territory, do NOT reshape here) \u2014 inbound_edges field (132), _edge_corpus (152-180), _address_matches_key (274-276), _compute_inbound_edges (279-300). fetch_graph reads surface.inbound_edges + Row.address; these become Address(kind,key)-typed under slice 1.",
     "est_loc_delta": "0"
    }
   ],
   "current_behavior": "`fetch_graph(vertex_path, *, kind=None, observer=None)` (fetch.py:966) projects the store as a directed graph. It calls `project(fetch_fold(vertex_path, kind=kind, observer=observer))` (fetch.py:1008) \u2014 note NO key param. The graph lens's own `fetch(vertex_path, kind=None, observer=None)` (lenses/graph.py:45-49) also omits key. Because `call_lens_fetch` (lens_resolver.py:513-540) passes only kwargs the fetch signature names, `_build_fold_fetch`'s `key=` (fold.py:422) is SILENTLY DROPPED for the graph lens. Verified on the live project vertex: `sl read project --lens graph --json` and `... --key architecture/ --json` are byte-identical (nodes 1312 / edges 1765 / dangling 166 / orphans 485). Single `--key` also escapes the dispatch inert-transform note because `_spec_has_dropped_transforms` (dispatch.py:105-131) excludes `queried_key`. `--kind` IS honored (fetch-level filter) but conflates: `--kind decision` gives dangling 310 (vs 166 unfiltered) because refs to kind-excluded-but-existing nodes are counted as `dangling` (fetch.py:1033-1034: `dangling = total_outbound - resolved_edges`). Chain paths are `list[str]` of addresses only \u2014 the DFS discards the predicate at fetch.py:902 (`targets = sorted({t for t, _ in adjacency.get(node, ())})`), so `_chain_line` (lenses/graph.py:74-92) renders `a \u2192 b \u2192 c` with no edge labels. Cycles are guarded (per-path back-edge skip, fetch.py:904) but never reported \u2014 no census key. The returned dict (fetch.py:1090-1103) exposes `dangling` and `unsourced_inbound` as single scalars with no dangling/filter-excluded/keyless breakdown and no `cycles`/`sccs` key. Graph tests (37) pass at baseline.",
   "planned_changes": [
    {
     "step": "(a) Honor --key, never silently drop",
     "description": "Widen `fetch_graph(vertex_path, *, kind=None, key=None, edge=None, observer=None)` and the graph lens `fetch(vertex_path, kind=None, key=None, edge=None, observer=None)` to accept key. Forward key into the node projection. RECOMMENDED shape: keep the in-scope node projection filtered by kind+key (matches current --kind behavior \u2014 pass key to fetch_fold OR filter Surface rows post-project), AND additionally fetch the FULL unfiltered vertex once (`fetch_fold(vertex_path, observer=obs)` \u2192 project \u2192 `global_node_addrs = {r.address for r in rows}`) purely to power the (e) classification. `call_lens_fetch` will now pass key to the widened graph fetch (honored); if a future non-graph composition fetch lacks a key param it still silently drops \u2014 that is fine for graph. For the honor-or-refuse posture on the CLI edge (e.g. --key on a lens whose fetch cannot honor it), the existing _lens_fetch_accepts_cursor refusal idiom (fold.py:367) is the template.",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/src/loops/commands/fetch.py",
      "/Users/kaygee/Code/loops/apps/loops/src/loops/lenses/graph.py"
     ]
    },
    {
     "step": "(b) --edge predicate selector",
     "description": "Add `--edge` to read_args.py (metavar=PREDICATE, comma-OR grammar mirroring --key; help: 'Restrict the graph to edges of this predicate (ref, or a declared typed-edge field; comma-OR)'). Thread args.edge through fold.py `_build_fold_fetch` into the `call_lens_fetch(...)` call ONLY (add `edge=edge`), NOT into the `fetch_fold(...)` fallback branch (fetch_fold has no edge param). In fetch_graph, when edge is set, restrict the outbound adjacency build (fetch.py:1015-1029) and census to edges whose `pred` is in the edge set \u2014 hubs re-rank by the filtered inbound, chains follow filtered edges, census lists only the selected predicate(s). Name deliberately avoids 'predicate' (owned by the inline field=value eq grammar).",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/read_args.py",
      "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/views/fold.py",
      "/Users/kaygee/Code/loops/apps/loops/src/loops/commands/fetch.py"
     ]
    },
    {
     "step": "(c) Retain predicate labels through the DFS",
     "description": "Change the chain representation to carry the edge predicate per hop. In `_longest_chains` (fetch.py:848), stop collapsing `{t for t, _ in ...}` at line 902 \u2014 walk `(target, predicate)` and record the predicate that entered each node. Emit chains as list of hops carrying (address, predicate_into_this_node) \u2014 e.g. path stays `list[str]` plus a parallel `predicates: list[str|None]` (first entry None, one per edge). Propagate through `_top_chains` (fetch.py:933) subpath logic (compare on address path only; carry predicates alongside) and the returned `chains` entries (fetch.py:1080, 1101). In `_chain_line` (lenses/graph.py:74-92) render `a --ref--> b --blocks--> c` (or a chosen glyph) instead of bare `\u2192`; keep the shed-to-`a \u2192 \u2026 \u2192 c` fallback and the truncated/leaf tails.",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/src/loops/commands/fetch.py",
      "/Users/kaygee/Code/loops/apps/loops/src/loops/lenses/graph.py"
     ]
    },
    {
     "step": "(d) SCC/cycle census in the returned dict",
     "description": "Add a strongly-connected-component pass (Tarjan or iterative Kosaraju, iterative to avoid recursion limits at 1300+ nodes) over the resolved `outbound` adjacency (fetch.py:1015-1029) AFTER it is built. Report non-trivial SCCs (size>1, plus self-loops) as a new dict key (proposed `cycles: [[addr, ...], ...]` or `sccs` with count) at fetch.py:1090-1103. Render a small census section in graph_view at DETAILED+ (mirroring the EDGES/ORPHANS sections) disclosing cycle count and members. On the live vertex today this should report 0 (near-DAG); the value is forward-required for batch-emit (thread:ref-graph-dag-assumption-audit).",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/src/loops/commands/fetch.py",
      "/Users/kaygee/Code/loops/apps/loops/src/loops/lenses/graph.py"
     ]
    },
    {
     "step": "(e) Partition dangling vs filter-excluded (+ keyless)",
     "description": "Replace the single `dangling = total_outbound - resolved_edges` (fetch.py:1033-1034) with a 3-way classification of each UNRESOLVED outbound ref: (1) filter_excluded \u2014 the ref's target resolves to a node in `global_node_addrs` (the full-vertex set from step a) but not in the in-scope `node_addrs`; (2) dangling \u2014 target resolves nowhere even globally (genuine dead ref, e.g. decision:ghost); (3) keyless \u2014 the SOURCE is keyless/collect (already surfaced inbound-side as unsourced_inbound \u2014 decide whether to also count outbound-side, see open question). Emit `dangling` (genuine only) + `filter_excluded` as separate counts (and optionally address lists) in the returned dict. Also partition orphans: a node that is isolated in-scope but connected in the full graph is `filter_isolated`, distinct from `orphans` (truly isolated globally) \u2014 reuse global_node_addrs + the global inbound_edges to test. Update graph_view counts line (lenses/graph.py:52-60, 202-207, 306-313) to disclose the split honestly.",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/src/loops/commands/fetch.py",
      "/Users/kaygee/Code/loops/apps/loops/src/loops/lenses/graph.py"
     ]
    },
    {
     "step": "Slice-1 coordination (typed Address)",
     "description": "The graph walk's node-membership tests (`source not in node_addrs`, `target not in node_addrs` at fetch.py:1020-1024) and the (e) classification (`target in global_node_addrs`) are string-set membership TODAY. Slice 1 retypes Row.address, Surface.inbound_edges (surface.py:132), Edge.address (fold_state.py:33) and the corpus matcher _address_matches_key (surface.py:274-276) to Address(kind,key). Structure BOTH membership tests as a single helper (e.g. `_resolves_to_node(addr, node_set)`) so that when slice 1 lands, the classification switches from suffix-string matching to Address equality (kind AND key) in ONE place \u2014 this is exactly the aliasing the slice-1 hypothesis flags (cross-kind key aliasing via suffix-only match). If slice 1 has already landed when this is implemented, build the helper directly on Address equality.",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/src/loops/commands/fetch.py"
     ]
    },
    {
     "step": "Regenerate goldens",
     "description": "The 8 tests.golden.test_grammar_parity[*-graph] snapshots change (chains line gains predicate labels from (c); counts line gains the dangling/filter-excluded breakdown from (e); a cycle-census line may appear). Run `uv run --package loops pytest apps/loops/tests/golden --update-goldens` and read the diff as the drift report (conftest.py:49 supports the flag).",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/tests/golden/goldens/tests.golden.test_grammar_parity/"
     ]
    }
   ],
   "test_plan": [
    "Unit (fetch_graph key honoring): build a fixture where --key selects a subgraph; assert `fetch_graph(vp, key='...')` returns FEWER nodes than unfiltered (proves the silent-drop is gone) and that the excluded-but-referenced targets land in filter_excluded, not dangling.",
    "Unit (--edge filtering): fixture with mixed ref + a declared typed edge (e.g. blocks); assert `fetch_graph(vp, edge='blocks')` census/hubs/chains contain only blocks edges and `edge='ref'` excludes the typed edge. Reuse the grammar fixture (write_grammar_fixture) which already has one typed edge candidate.",
    "Unit (chain predicate retention): extend TestTraversal \u2014 assert a chain over mixed predicates carries the per-hop predicate labels (a\u2192(ref)\u2192b\u2192(blocks)\u2192c), and that _top_chains subpath-dedup still keys on the address path.",
    "Unit (SCC census): feed _longest_chains/fetch_graph a cyclic adjacency (a\u2192b\u2192c\u2192a) and assert the returned dict reports the SCC {a,b,c}; assert a pure DAG reports zero cycles; assert the existing cycle-does-not-hang guarantee still holds (test_cycle_does_not_hang, test_cycle_memo_not_poisoned_by_visit_order must stay green).",
    "Unit (dangling partition): extend the _dangling_fixture (decision:ghost is genuine-dangling) with a case where a --kind/--key filter excludes an EXISTING target; assert dangling counts only decision:ghost and filter_excluded counts the excluded-but-existing target. Assert the arithmetic still reconciles (edges + unsourced_inbound == summed hub inbound stays true; add a reconciliation assert for the new split).",
    "Register parity: extend the existing assert_register_parity graph test so the new counts-line breakdown and chain predicate labels appear faithfully on BOTH the piped and TTY registers (the graph lens is register-split \u2014 this is the standing requirement in apps/loops/CLAUDE.md).",
    "Golden: run `uv run --package loops pytest apps/loops/tests/golden -k graph` after --update-goldens; eyeball the diff to confirm only intended changes (predicate labels + counts breakdown), no accidental drift.",
    "Full gate: `uv run --package loops pytest apps/loops/tests` green (baseline 37 in test_graph.py + goldens).",
    "Live smoke via installed CLI (per CLAUDE.md \u2014 run `uv tool install . -e` first): `sl read project --lens graph --key architecture/ --json` must now differ from the unfiltered run (nodes shrink), `sl read project --lens graph --edge ref` restricts the census, and `--kind decision --json` dangling drops sharply as filter_excluded absorbs the ~310-166 conflation."
   ],
   "acceptance_criteria": [
    "`sl read project --lens graph --key architecture/` is NO LONGER byte-identical to the unfiltered graph \u2014 --key is honored (node set shrinks to the architecture/ prefix), closing friction:graph-fetch-drops-key.",
    "A --key/--edge that cannot be honored is refused with a teaching message (honor-or-refuse, the 184dfce posture), never silently dropped.",
    "`--edge <predicate>` restricts hubs, chains, and census to the selected edge predicate(s); the name does not collide with the inline field=value 'predicate' grammar.",
    "Chains render per-hop predicate labels on both registers (e.g. a --ref--> b --blocks--> c), retained through the DFS rather than discarded at the walk boundary.",
    "The returned dict carries an SCC/cycle census; a near-DAG reports zero, a cyclic fixture reports the component \u2014 and the existing no-hang cycle guard is preserved.",
    "The returned dict splits the former single `dangling` into genuine-dangling vs filter-excluded (and discloses keyless/unsourced honestly); on `--kind decision` the genuine-dangling count is far below today's 310, with the difference attributed to filter_excluded.",
    "Register parity holds for all new fields; grammar-parity goldens regenerated with only intended diffs; full apps/loops test suite green.",
    "Node-membership + classification go through a single helper so slice 1's Address(kind,key) retyping swaps string-suffix matching for Address equality in one place."
   ],
   "risks": [
    "Behavior change to the EXISTING --kind path: today --kind decision reports dangling=310; after (e) most of that becomes filter_excluded. This is the intended fix (the 268-conflation the design cites) but it changes a shipped output shape \u2014 the graph goldens and any downstream consumer of the flat `dangling` scalar must be updated in the same change (dissolution-sweep the residue).",
    "Inbound-count / chain / tier scope under a filter stays SUB-SCOPE (a filtered node's \u2190N counts only in-scope sources, because project() computes the corpus over the filtered FoldState). This is a pre-existing caveat (documented for tiers at surface.py:532) and is NOT changed here \u2014 but combined with the new global-node-set classification it can look inconsistent (global for dangling/orphan classification, sub-scope for counts). Document it as named residue, or resolve per the open question below.",
    "Adding a second full-vertex fetch (for global_node_addrs) doubles the fold cost of a filtered graph read. On the 1312-node live vertex the fold is fast (<1s), but note the extra pass; a single unfiltered project + in-memory scope-filter (global-compute approach) would avoid the double fetch if the arbiter chooses that shape.",
    "Chain representation change (list[str] \u2192 path + predicates) ripples through _top_chains subpath dedup and every chains-consumer (test_graph.py assertions compare exact chain dicts, the golden parity harness, _chain_line). Keep `path` a plain address list and add predicates as a PARALLEL field to minimize breakage to existing `{path, truncated}` consumers.",
    "SCC over 1300+ nodes must be iterative \u2014 a recursive Tarjan risks Python's recursion limit (the same reason _longest_chains has a depth cap of 128).",
    "`--edge` given without `--lens graph` has no meaning on the default fold route; if not explicitly refused it silently no-ops (the very failure mode this slice fixes for --key). Decide the without-graph behavior (see open questions)."
   ],
   "dependencies": [
    "Slice 1 (typed-address, Address(kind,key)) \u2014 hypothesis:refs-as-typed-address-dataclass. Reshapes the address-string sites this walk reads: Row.address, Surface.inbound_edges (surface.py:132), Edge.address (fold_state.py:33), and the suffix-only matcher _address_matches_key (surface.py:274-276). This slice must route node-membership and the (e) classification through a single helper so the swap to Address equality is one-line when slice 1 lands. Not a hard blocker \u2014 can be built on today's strings if slice 1 is not yet merged \u2014 but the helper structure is mandatory to avoid rework.",
    "184dfce (read-router temporal-flag refusal) \u2014 the honor-or-refuse idiom; test template at apps/loops/tests/test_read_router_temporal_guard.py. The runtime analogue for lens fetches is _lens_fetch_accepts_cursor (fold.py:367).",
    "thread:ref-graph-dag-assumption-audit (open) \u2014 item (d) is the forward-required half: cycles become legal once batch-emit ships; the census makes them visible.",
    "painted (PyPI) rendering primitives used by graph_view (Block, join_vertical, Style, Zoom) \u2014 no version bump required; the chain-label and counts-line changes are pure loops-side lens edits."
   ],
   "open_questions": [
    "FILTER SEMANTICS FORK: when --key/--kind narrows the graph, are hub inbound counts, chains, and tiers computed over the in-scope SUB-GRAPH (current --kind behavior) or over the FULL graph with the scope applied only as a display/classification overlay? Item (e)'s dangling-vs-filter-excluded split REQUIRES the global node set either way, but whether inbound/chains go global is a real fork. RECOMMENDATION: keep node projection sub-scope (least surprising, matches shipped --kind), add the full-vertex node set solely for (e) classification. Confirm, or choose global-compute (single projection, scope as overlay \u2014 cleaner but changes \u2190N semantics under a filter).",
    "OUTBOUND-KEYLESS TAXONOMY: item (e) says 'partition orphans by keyless-vs-dangling'. `keyless` (source has no address) is today disclosed INBOUND-side as `unsourced_inbound`. Should the outbound-unresolved partition ALSO carry a `keyless` bucket (source-keyless refs), or does keyless stay purely an inbound-side disclosure and the outbound split be just {dangling, filter_excluded}? This decides the exact dict-key set \u2014 arbiter-level because it fixes the wire contract.",
    "--edge WITHOUT --lens graph: refuse (honor-or-refuse, consistent with 184dfce), silently no-op, or emit the dispatch inert-transform note? RECOMMENDATION: refuse with a teaching message pointing at --lens graph, since silent no-op is the exact defect class this slice eliminates. Confirm the posture."
   ],
   "measures": [
    "genuine-dangling vs filter_excluded counts on `--kind decision` (today: 310 conflated; target: genuine << 310)",
    "cycle/SCC count on the live project vertex (expected 0, near-DAG)",
    "nodes returned for `--lens graph --key architecture/` (today 1312 = silent-drop; target: shrinks to the architecture/ subset)",
    "LOC delta of the classification helper (single chokepoint for slice-1 Address swap)"
   ]
  },
  {
   "slice": "Slice 4 \u2014 canonical review projection + cursor/decl-generation disclosure",
   "thesis": "Add a deterministic, diffable review projection of a vertex's folded state \u2014 sorted by (kind,key), whitelisted to only emit-derived fields, with each fact's signature carried verbatim from the store, under a header disclosing the read's cursor position and the declaration generation that interpreted the facts. This is a new read-only surface built entirely over the EXISTING fold fetch + declaration-status machinery; it deliberately stops short of the loops-go SPEC \u00a710 total-dump (witness-ordered every-row JSONL, JCS byte-determinism, rebuild round-trip, chain) which is gated on GlobalReceiptPosition. Two supporting disclosures ride along: extending the 0.8.0 cursor mode-line to HEAD reads (today only --at/--as-of emit it), and a declaration-generation fingerprint that exists nowhere today.",
   "files": [
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/surface.py",
     "role": "Home of the new canonical review encoder (to_review) + its deterministic (kind,key) sort and field whitelist. Sits beside the existing to_dict encoder (lines 1017-1040). Painted-free leaf; must stay so.",
     "est_loc_delta": "+120"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/views/fold.py",
     "role": "Add the --review short-circuit branch in run() (same pattern as --why at 581-594 and --diff at 599-600) that fetches the fold, resolves cursor (head or --at/--as-of), computes decl generation, and emits the canonical JSON. Also add head-cursor resolution for the default (no --at/--as-of) path via a small extension around lines 605-616.",
     "est_loc_delta": "+95"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/read_args.py",
     "role": "Declare the --review flag (store_true) alongside --at/--as-of/--diff (lines 109-133) so the fold parser and completion/-h see it.",
     "est_loc_delta": "+6"
    },
    {
     "path": "/Users/kaygee/Code/loops/libs/engine/src/engine/declaration.py",
     "role": "New declaration_generation() helper (reuses resolve_declaration_documents at 219-431 and load_declaration_status at 445-515) returning the generation fingerprint + status + lineage. Honest across both the adopted-store and pre-genesis-file residences.",
     "est_loc_delta": "+55"
    },
    {
     "path": "/Users/kaygee/Code/loops/libs/engine/src/engine/store_reader.py",
     "role": "New read-only fact_signatures(ids)->dict[id,str|None] helper (SELECT id, signature FROM facts WHERE id IN (...)); guarded on the signature column existing (mirror the PRAGMA probe at 211-224). Verbatim column read, never recompute.",
     "est_loc_delta": "+30"
    },
    {
     "path": "/Users/kaygee/Code/loops/libs/engine/src/engine/__init__.py",
     "role": "Export declaration_generation and fact_signatures (or a store-path wrapper).",
     "est_loc_delta": "+4"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/views/read.py",
     "role": "Optionally refuse --review combined with --facts/--ticks/--diff (fold-route-only concern), mirroring the --at/--diff route-guard at 56-68.",
     "est_loc_delta": "+8"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/tests/test_review.py",
     "role": "New test file: determinism (byte-identical across two reads of unchanged state), whitelist enforcement (no salience/tier/inbound/window keys), signature-per-fact inclusion + null-on-unsigned, header cursor + decl-generation shape, aggregate/pre-genesis degradation.",
     "est_loc_delta": "+160"
    },
    {
     "path": "/Users/kaygee/Code/loops/apps/loops/tests/golden/",
     "role": "If head-cursor disclosure is turned on for the DEFAULT TTY read (arbiter fork), the fold goldens churn and must be regenerated with --update-goldens.",
     "est_loc_delta": "+0/regen"
    }
   ],
   "current_behavior": "read --json (gate-pass path) emits surface.to_dict (surface.py:1017-1040): top-level keys {vertex, rows, schema, unfolded, source_facts, window} \u2014 verified empirically. Three concrete gaps vs a review artifact: (1) TRAVERSAL ORDER is fold order, not canonical \u2014 project() walks sections in vertex DECLARATION order (vertex_reader.py:892-898 'Declaration order from AST') and items within a by-fold in items_raw.values() = fold-state dict insertion / first-seen-key (emit) order (vertex_reader.py:927-928); surface.py:552-566 documents this as 'FAITHFUL FOLD ORDER'. Two stores with identical folded state but different emit order diff spuriously. (2) DIFF-NOISE fields: _row_to_dict (surface.py:977-1002) emits salience, tier, inbound, inbound_predicates, depth, via_anchor, granularity \u2014 all read-time DERIVED (inbound/salience change when OTHER facts emit refs; tier is a population quantile via _assign_tiers surface.py:532-542) \u2014 plus to_dict includes top-level window (a read-time slice artifact) and schema ranking hints. (3) OMITS SIGNATURE: _row_to_dict has no signature; atoms.FoldItem carries none (fold_state.py:37-82); signatures live ONLY in the store column facts.signature (sqlite_store.py:283-291, nullable), keyed by fact id, never threaded into the fold/Surface path (verified: read --json row has no 'signature' key; project store has 658/3123 signed facts). Cursor disclosure: fold.py:605-616 builds cursor_meta ONLY when args.at or args.as_of; dispatch merges it into JSON (dispatch.py:310-328) and the fold lens prepends cursor_mode_line (lenses/fold.py:226-245, wired at 414-421). A plain head read builds no cursor_meta, so no cursor line renders (verified: 0 cursor lines on `sl read project`). --at head DOES resolve a witness position even on a pre-genesis/unadopted store (verified: seq 3123, status file-pre-genesis, anchor present) via resolve_at_address(store,'head') -> resolve_witness_position (witness_address.py:178-179). Declaration generation: NOTHING today discloses which generation of declarations produced a read; load_declaration_status (declaration.py:445-515) returns only a coarse status label ('store'|'file-pre-genesis'|'unhistorized'|'aggregate-head', declaration.py:434-442). Project store is pre-genesis (0 _decl rows, no store_meta) \u2014 the dominant live case.",
   "planned_changes": [
    {
     "step": "1. Confirm the SPEC \u00a710 boundary and encode it in the brief/docstrings",
     "description": "Read loops-go/SPEC.md \u00a79 (927-1187) and \u00a710 (1189-1246). \u00a710.1 defines the DUMP as ONE JSONL stream of EVERY row (facts + declaration events + ticks) in WITNESS order (\u00a78.4); \u00a710.2 makes it JCS byte-deterministic; \u00a710.3 requires rebuild(dump(S)) chain-validity + dump(rebuild(dump(S))) byte-identity; \u00a710.4 adds per-vertex residence modes + transit receipts; \u00a710.5 sequences all \u00a710 vectors AFTER the _canonical_bytes->JCS migration (a hard constraint) and the whole layer is PROVISIONAL, gated on GlobalReceiptPosition (loops-go coordination batch). The BUILDABLE CORE STOPS SHORT of ALL of that: the review projection encodes FOLDED ENTITY state (one row per kind/key), sorted by (kind,key) for human diffability \u2014 NOT witness order, NOT every row, NO ticks, NO declaration-event rows, NO chain. Its determinism is 'same folded state -> same bytes' (a review property), explicitly NOT \u00a710.2 canonical-interchange bytes; it is NOT an encoding and is NOT rebuildable (\u00a710.3 does not apply). Building anything witness-ordered/rebuildable/JCS-canonical here would fork the interchange contract \u2014 refuse it. Docstrings must say 'review snapshot, not interchange dump'.",
     "files": [
      "/Users/kaygee/Code/loops-go/SPEC.md"
     ]
    },
    {
     "step": "2. Add engine helper: fact_signatures(store_path, ids) -> {id: signature|None}",
     "description": "Read-only batched lookup of the facts.signature column by id. Open the store read-only (mode=ro, mirroring declaration._open_readonly at declaration.py:201-216 and the slice-2 read-purity posture); PRAGMA-probe the signature column (mirror store_reader.py:211-224) and return all-None if the column is absent (pre-signature store). CUSTODY: read the column verbatim, never recompute or re-sign (matches sqlite_store append signature_override posture at 492-520). This is the ONLY source of per-fact signatures for the projection \u2014 the fold path drops them entirely.",
     "files": [
      "/Users/kaygee/Code/loops/libs/engine/src/engine/store_reader.py",
      "/Users/kaygee/Code/loops/libs/engine/src/engine/__init__.py"
     ]
    },
    {
     "step": "3. Add engine helper: declaration_generation(vertex_path, *, at=None, as_of=None) -> dict",
     "description": "Reuse load_declaration_status (declaration.py:445-515) for the status label and residence detection, and resolve_declaration_documents (219-431) for the folded declaration document set on adopted stores. Return {status, lineage, generation, head?}. RECOMMENDED minimal mechanism (see open question): generation = a plain sha256 fingerprint over a deterministically-serialized projection of the resolved declaration (the document set on an adopted store; the file's parsed declaration content on a pre-genesis store) \u2014 ONE fingerprint honest in BOTH residences, answering 'did the ontology change between two reads'. Label it a REVIEW fingerprint, NOT \u00a710.2 JCS canonical bytes. On adopted stores ALSO surface head = id (ULID) of the latest-applied self-lineage _decl.* event within the cutoff (rebuild-stable, unlike rowid). On pre-genesis stores (0 _decl rows \u2014 the project store today) lineage=null, head=null, status='file-pre-genesis'. Aggregates report status='aggregate-head' (membership is current file state, declaration.py:482-492).",
     "files": [
      "/Users/kaygee/Code/loops/libs/engine/src/engine/declaration.py",
      "/Users/kaygee/Code/loops/libs/engine/src/engine/__init__.py"
     ]
    },
    {
     "step": "4. Add surface.to_review(state, *, header) -> dict \u2014 the canonical encoder",
     "description": "Operate on the fetched FoldState (NOT the derived Surface \u2014 the whitelist is exactly atoms.FoldItem's own fields, so no Surface scalars are needed). Iterate state.sections ONLY (ignore walked ref-graph reach \u2014 that's a read-time graph, not folded self-state). For each item build a row: {kind, key (the fold-key value or null), key_field, payload (with SORTED keys), id, ts, observer, origin, n, refs (sorted), edges [{predicate,address}], signature (verbatim from fact_signatures by item.id, or null)}. WHITELIST = FoldItem fields + address + signature; EXCLUDE every read-time derivation (salience, tier, inbound, inbound_predicates, depth, via_anchor, granularity) and the Surface window/schema-ranking. Sort rows deterministically by (kind, key or '', id) so keyless collect rows order by id. Emit {header, facts: [...]} with json.dumps(..., sort_keys=True) so two reads of unchanged state are byte-identical. Keep surface.py painted-free; the signature lookup is injected by the caller (pass signatures dict in) OR call the engine helper via a local import like _indexed_kinds does (surface.py:744-758) \u2014 prefer injecting the dict so surface stays a leaf.",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/src/loops/surface.py"
     ]
    },
    {
     "step": "5. Wire --review as a fold-route short-circuit in fold.py::run",
     "description": "Declare --review in read_args.add_read_args (read_args.py:109-133). In fold.py::run, after cursor resolution, add a branch (patterned on _run_why 581-594 / _run_diff 599-600 which own their JSON) that: resolves the cursor (head when no --at/--as-of via step 6; else the already-resolved at_position/as_of_ts + cursor_meta), calls fetch_fold(vertex_path, kind=kind, key=fetch_key, at=at_position, as_of=as_of_ts) for the FoldState, batches signatures via engine.fact_signatures using every item.id, computes engine.declaration_generation(vertex_path, at=..., as_of=...), assembles header={vertex, cursor: cursor_meta, declaration: <gen>}, and prints surface.to_review(state, header=header). --review is JSON-only (a machine artifact); ignore/refuse TTY-render flags. In read.py refuse --review with --facts/--ticks/--diff (fold-route-only, mirror 56-68).",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/views/fold.py",
      "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/read_args.py",
      "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/views/read.py"
     ]
    },
    {
     "step": "6. Extend cursor disclosure to HEAD reads",
     "description": "Add a helper in fold.py that, when neither --at nor --as-of is set AND the vertex is a single store (not aggregate \u2014 reuse witness_address.is_aggregate_vertex, witness_address.py:43-48), resolves the head position via resolve_at_address(store_path,'head') + load_declaration_status(vertex_path, at=head_pos) and builds a cursor_meta dict shaped like the --at path (fold.py:332-343) but flagged as head (e.g. address='head', add a 'head': true marker) so the mode-line can phrase 'at head' vs an explicit rewind. Verified this resolves even on pre-genesis/unadopted stores (durable_handle null, portable false, unadopted true \u2014 the existing honesty fields already carry the caveat). Aggregates: skip (no witness head) \u2014 emit no cursor or a mode='head' status='aggregate-head' with no position. For the --review header this head cursor_meta is ALWAYS populated (in scope, no golden churn \u2014 new surface). Whether to ALSO thread it into the DEFAULT TTY/JSON read (so `sl read project` grows a mode-line) is the arbiter fork below; if yes, thread cursor_meta into render_context exactly as fold.py:733-736 and regenerate fold goldens.",
     "files": [
      "/Users/kaygee/Code/loops/apps/loops/src/loops/cli/views/fold.py"
     ]
    }
   ],
   "test_plan": [
    "Determinism: emit the same logical fold twice in different emit order into two temp stores (or reuse a fixture); assert to_review bytes are identical (sort_keys + (kind,key) sort). Contrast: assert to_dict rows differ in order for the same pair (documents the gap).",
    "Whitelist: assert no row in --review output carries salience/tier/inbound/inbound_predicates/depth/via_anchor/granularity, and the top level carries no window/schema; assert it DOES carry payload/id/ts/observer/origin/n/refs/edges/signature.",
    "Signature-per-fact: on a store with mixed signed/unsigned facts (project store has 658/3123), assert signed folded rows carry the verbatim column value and unsigned rows carry null; assert the signature equals a direct SELECT (no recomputation).",
    "Header cursor at head: `sl read project --review` header.cursor.mode=='witness'/head marker, address=='head', status=='file-pre-genesis', unadopted true, portable false, anchor present \u2014 matches the empirically-confirmed --at head shape.",
    "Header cursor with --at/--as-of: `--review --at seq:N` and `--review --as-of 30d` carry the corresponding cursor_meta; `--review --at X --as-of Y` refused (A8, read.py:124-130).",
    "Declaration generation: pre-genesis store -> {status:'file-pre-genesis', lineage:null, generation:<hash>}; two reads of an unchanged store -> identical generation; editing the .vertex file changes the fingerprint. (If an adopted-store fixture exists, assert head = latest _decl id.)",
    "Aggregate degradation: `--review` on a combine/discover vertex reports status 'aggregate-head' and no witness head position (no crash).",
    "Head disclosure on default read (only if the arbiter turns it on): golden regeneration + a targeted test that `sl read project` (no flags) now prints a cursor mode-line and `--json` carries a cursor field.",
    "Read-purity: assert --review opens stores read-only and does not mutate the store (file mtime / FTS unchanged) \u2014 consistent with slice-2 posture.",
    "Router guard: `--review --facts`, `--review --ticks`, `--review --diff A..B` are refused with a teaching message.",
    "Run the existing suites unchanged: uv run --package loops pytest apps/loops/tests (esp. test_surface.py, test_fold_view_cursor.py, golden/test_fold_surface_paths.py) and uv run --package engine pytest libs/engine/tests. After CLI changes, uv tool install . -e then exercise `sl read project --review` directly."
   ],
   "acceptance_criteria": [
    "`sl read <vertex> --review` emits JSON {header, facts:[...]} where facts are sorted by (kind,key) and byte-identical across repeated reads of unchanged folded state.",
    "Each fact row carries payload (sorted keys), id, ts, observer, origin, n, refs, edges, and signature (verbatim store column, or null when unsigned) \u2014 and carries NONE of salience/tier/inbound/inbound_predicates/depth/via_anchor/granularity/window/schema.",
    "header.cursor discloses the read's position: head for a plain --review, or the resolved witness/event-time position for --at/--as-of, using the existing cursor_meta shape (status, unadopted, portable, anchor, durable_handle).",
    "header.declaration discloses status + a generation fingerprint that is stable across unchanged reads and changes when the declaration changes, honest on both adopted-store and pre-genesis-file residences.",
    "Signatures are read verbatim; no code path re-signs or recomputes a signature. No tick-chain fields are included. An unadopted store is disclosed as unadopted/non-portable, never silently adopted.",
    "The projection is strictly read-only (stores opened mode=ro; no store mutation) and stops short of SPEC \u00a710: no witness-ordered every-row stream, no JCS byte-determinism claim, no rebuild round-trip, no chain \u2014 docstrings state this explicitly.",
    "HEAD reads can emit the cursor disclosure (the mechanism exists and is wired into the review header); existing --at/--as-of behavior is unchanged.",
    "Full existing test + golden suites pass (goldens regenerated only if the arbiter enables default-read head disclosure)."
   ],
   "risks": [
    "Golden blast radius: if head-cursor disclosure is turned on for the DEFAULT read, every fold golden (the most-used command's output) churns \u2014 a large, reviewable-but-noisy diff. Mitigation: keep default-read head disclosure behind the arbiter fork; the review header carries head cursor with zero golden impact.",
    "surface.py is a painted-free LEAF that the lens depends on; adding a signature lookup there could pull an engine import to module scope. Mitigation: inject the signatures dict from the caller (fold.py), keep the engine call out of surface.py, or use the same in-function local-import pattern as _indexed_kinds (surface.py:744-758).",
    "Determinism traps: json.dumps default key order + float ts repr + payload nested dicts. Mitigation: sort_keys=True end to end, sort payload/refs, and a round-trip determinism test. Do NOT reach for JCS/RFC-8785 here \u2014 that is \u00a710.2 territory sequenced after the _canonical_bytes->JCS migration (SPEC \u00a710.5); a plain sorted sha256 fingerprint is sufficient and must be labeled review-only.",
    "Declaration-generation semantics: a content hash over the file (pre-genesis) vs the document set (adopted) hashes two different spaces; a naive file hash (mtime/whitespace) would over- or under-report change. Mitigation: hash the PARSED declaration projection, not raw bytes, so cosmetic edits don't churn and semantic edits do.",
    "FoldItem.id for a by-fold is the LATEST contributing fact (fold_state.py:51-54); its signature is the winning fact's authorship \u2014 correct for a folded-head review, but the impl agent must not assume it signs the whole key history.",
    "Aggregate/storeless vertices have no witness head and no single store for signatures \u2014 the review path must degrade honestly (status aggregate-head, null signatures/generation) rather than crash, mirroring the existing --at aggregate refusal (witness_address.py:68-103)."
   ],
   "dependencies": [
    "Slice 2 (FTS read-purity) is assumed landed: the review projection is a snapshot-for-review and must be strictly read-only. The encoder itself triggers no FTS (no --match), but its signature + declaration-generation lookups must use read-only connections consistent with slice-2's 'store reindex is the sole FTS writer / read-only vertex_search' posture.",
    "Existing 0.8.0 temporal-cursor machinery: fold.py::_resolve_cursor (282-361), witness_address.resolve_at_address (151-246), engine.load_declaration_status / resolve_declaration_documents (declaration.py:219-515), lenses/fold.cursor_mode_line (226-245), dispatch cursor merge (310-328). Reused, not rebuilt.",
    "Custody substrate already implemented: facts.signature column (sqlite_store.py:283-291), signature_override verbatim-preserve posture (492-520), witness adoption marker / durable_handle (witness.py, declaration._read_own_lineage 175-188).",
    "GlobalReceiptPosition + the loops-go SPEC \u00a710 total-dump are a HARD NON-dependency: the buildable core must not depend on or anticipate them.",
    "After CLI changes: uv tool install . -e, then exercise `sl read project --review` directly (per CLAUDE.md install-path discipline)."
   ],
   "open_questions": [
    "Declaration-generation mechanism (the one genuine fork): (a) a content-hash fingerprint over the resolved declaration \u2014 works honestly in BOTH residences and gives a single comparable value, but introduces a new hashing contract that must be carefully kept distinct from SPEC \u00a710.2 JCS canonical bytes; vs (b) the decl-head event id (ULID of the latest-applied self-lineage _decl.* event) \u2014 simpler and rebuild-stable, but store-only, so pre-genesis (the dominant case today) has no comparable value and can only report a 'file' sentinel. Recommend (a) as the default (labeled a review fingerprint), with (b)'s head id ALSO surfaced on adopted stores. Needs arbiter sign-off because it defines a new disclosed contract field.",
    "Head-cursor disclosure scope: should the head mode-line render on EVERY default read (`sl read project` gains a persistent cursor line \u2014 the design's symmetric-honesty aim per cursor_mode_line's docstring, but all fold goldens churn and the most-used command's output changes), or ONLY inside the --review header and --json cursor field (default TTY read visually unchanged, lower blast radius)? Recommend review-header + JSON disclosure now, and treat always-on TTY head disclosure as a separate opt-in the arbiter can flip."
   ],
   "measures": [
    "Byte-determinism: two --review reads of unchanged folded state produce identical bytes (assert equality).",
    "Whitelist coverage: count of forbidden keys (salience/tier/inbound/inbound_predicates/depth/via_anchor/granularity/window/schema) present in --review output == 0.",
    "Signature coverage: for the project store, signed folded rows carrying a verbatim signature vs total (baseline 658/3123 facts signed) \u2014 signed rows must carry the column value, unsigned must carry null.",
    "Declaration-generation stability: fingerprint unchanged across N repeated reads of an unedited store; changed after one .vertex edit."
   ]
  },
  {
   "slice": "Slice 5 \u2014 declarative entity lifecycle (unified kind-level lifecycle clause: declaration + fold-view hide + folded-state validate warn)",
   "thesis": "Add ONE kind-level KDL clause (working name `lifecycle`) that declares a kind's lifecycle field and its active/terminal state set, subsuming the three previously-parallel proposals: the domain-neutral lifecycle-field marker (grammar-domain-neutrality), the fold-view hide of terminal-status rows (status-aware-kind-budget, now the first tenant not a sibling), and the enum home in the kind-defined declaration document (payload-constraints-in-declarations). The clause flows through the existing declared-semantics pipeline (AST -> declaration document -> FoldSection -> Surface KindView -> budget()), hides inactive entities from the DEFAULT fold view only, with honest Window.limited_by provenance, `--all` defeat, and auto-disable when an explicit lifecycle-field predicate is present. Inactive nodes keep their salience contribution and stay reachable via --refs (the hide is a budget-stage projection over primary rows, applied strictly AFTER project() has materialized salience/tiers/inbound-edges over the full population). A folded-state active-targets-inactive warn is added to the `loops validate` surface. SPEC-9 pricing (below) shows this lands loops-side behind a documented spec-delta note without blocking the loops-go oracle.",
   "thesis_note": "n/a",
   "current_behavior": "Kind-block clauses parsed today are fold/boundary/search/parse/preview/edge only \u2014 libs/lang/src/lang/loader.py:384-443 (`_load_loop_def`); there is no lifecycle/hide clause. `LoopDef` (libs/lang/src/lang/ast.py:501-511) carries folds/boundary/search/parse/preview_fields/edges, no lifecycle. The kind-defined declaration document round-trip (`_loop_def_to_payload`/`_loop_def_from_payload`, libs/lang/src/lang/document.py:461-485) serializes exactly those six facets \u2014 this is the SPEC \u00a79.2 `_decl.kind-defined` payload schema. `budget()` (apps/loops/src/loops/surface.py:873-914) implements limit/last/salience_window slices and records Window.limited_by in {\"limit\",\"last\",\"salience\",None} (Window docstring surface.py:115); it has NO status/lifecycle hide, and `salience_window` is a latent param wired to no flag (grep: surface.py:878 only). Salience/inbound/tier are materialized ONCE in project() over ALL rows (surface.py:602 salience, 642 `_assign_tiers`, 657 inbound_edges) before any budget transform runs \u2014 so a post-project row-drop naturally preserves the edge-position invariant. FoldSection (libs/atoms/src/atoms/fold_state.py:112-123) carries preview_fields + edge_fields but no lifecycle; vertex_reader builds it from loop_def at libs/engine/src/engine/vertex_reader.py:934-961. `--all` does NOT exist in the read grammar (grep: present only for ls/population, ls_args.py:59, population.py:43 \u2014 NOT read_args.py). `loops validate` (apps/loops/src/loops/commands/devtools.py:20-107 + lenses/validate.py) validates FILE SYNTAX ONLY via lang.validate(ast) \u2014 it never opens a store or inspects folded state / edge targets, so no folded-state validate surface exists yet. loops-go has ZERO \u00a79 declaration-layer code (grep for kind-defined/_decl/lifecycle/declaration across loops-go/atoms,store,internal returns nothing); SPEC.md marks \u00a79 PROVISIONAL/design-led and only \u00a71-6 atoms are oracle-certified.",
   "files": [
    {
     "path": "libs/lang/src/lang/ast.py",
     "role": "Add a frozen `LifecycleDecl` dataclass (field name + active-or-terminal state set), mirroring EdgeDecl (ast.py:486-499); add `lifecycle: LifecycleDecl | None = None` to LoopDef (ast.py:501-511).",
     "est_loc_delta": "+15"
    },
    {
     "path": "libs/lang/src/lang/loader.py",
     "role": "Add an `elif child.name == \"lifecycle\":` branch in _load_loop_def (near the `edge` branch at loader.py:418-441): parse `lifecycle \"<field>\" active=\"...\"` (or terminal=), reject duplicates/malformed. Add the AST import.",
     "est_loc_delta": "+25"
    },
    {
     "path": "libs/lang/src/lang/document.py",
     "role": "THE SPEC-9 TOUCH POINT. Add `\"lifecycle\": ...` to _loop_def_to_payload (document.py:461-471) and `lifecycle=p.get(\"lifecycle\")`-style reconstruction to _loop_def_from_payload (474-485). `.get()` default preserves forward/backward compat. Do NOT bump DECLARATION_PROTOCOL_VERSION (document.py:151).",
     "est_loc_delta": "+12"
    },
    {
     "path": "libs/lang/src/lang/validator.py",
     "role": "Optional syntactic check in validate_vertex_file loop-def loop (validator.py:348-365): lifecycle field non-empty, active/terminal set non-empty. NOT the semantic active-targets-inactive check (that is the CLI validate surface).",
     "est_loc_delta": "+8"
    },
    {
     "path": "libs/atoms/src/atoms/fold_state.py",
     "role": "Add a lang-type-free lifecycle field to FoldSection (fold_state.py:112-123), mirroring edge_fields \u2014 e.g. `lifecycle: tuple[str, tuple[str, ...]] | None = None` (field name + active/terminal values).",
     "est_loc_delta": "+6"
    },
    {
     "path": "libs/engine/src/engine/vertex_reader.py",
     "role": "Extract loop_def.lifecycle into the new FoldSection field where edge_specs/preview_fields are extracted (vertex_reader.py:934-961).",
     "est_loc_delta": "+6"
    },
    {
     "path": "apps/loops/src/loops/surface.py",
     "role": "Add lifecycle to KindView (surface.py:52-59); populate it in project() from the FoldSection (surface.py:572-578); add `\"status\"` to Window.limited_by docstring + optional hidden-count field to Window (105-119) and _window_to_dict (1005-1014); add the hide to budget() (873-914) reading per-kind lifecycle from surface.schema \u2014 filter primary rows whose lifecycle-field value is terminal/not-active, set limited_by=\"status\", scoped to depth==0/level==key rows (never walked --refs rows).",
     "est_loc_delta": "+45"
    },
    {
     "path": "apps/loops/src/loops/cli/operation.py",
     "role": "Add `show_all: bool = False` to SurfaceSpec (operation.py:38-63) fed from --all.",
     "est_loc_delta": "+2"
    },
    {
     "path": "apps/loops/src/loops/cli/read_args.py",
     "role": "Add a `--all` store_true flag to the read parser (does not exist today).",
     "est_loc_delta": "+5"
    },
    {
     "path": "apps/loops/src/loops/cli/views/fold.py",
     "role": "Pass show_all=args.all_ into SurfaceSpec assembly (fold.py:669-683).",
     "est_loc_delta": "+2"
    },
    {
     "path": "apps/loops/src/loops/cli/dispatch.py",
     "role": "In _project_surface budget step (dispatch.py:177-178), gate the lifecycle hide: apply UNLESS spec.show_all, OR (per-kind) the kind's lifecycle field appears in spec.where (auto-disable on explicit predicate). Read the lifecycle field name from the projected surface.schema.",
     "est_loc_delta": "+15"
    },
    {
     "path": "apps/loops/src/loops/lenses/fold.py",
     "role": "Surface the hidden count in the fold footer, mirroring the window.unindexed footer (fold.py:610-616) and the `(N more)` collapse (884-886): e.g. `(N inactive hidden \u2014 --all)`.",
     "est_loc_delta": "+12"
    },
    {
     "path": "apps/loops/src/loops/commands/devtools.py",
     "role": "VALIDATION HOME. Extend _run_validate (devtools.py:20-107) with a folded-state pass: when a store is resolvable, fold it, and for each active entity carrying an edge/ref to a target whose lifecycle field is terminal/inactive, collect a warn. This is net-new folded-state surface (see open question on scope).",
     "est_loc_delta": "+40"
    },
    {
     "path": "apps/loops/src/loops/lenses/validate.py",
     "role": "Render the active-targets-inactive warns (validate.py:17-64) as a non-fatal WARN tier distinct from the syntax pass/fail rows.",
     "est_loc_delta": "+18"
    },
    {
     "path": "/Users/kaygee/Code/loops-go/SPEC.md",
     "role": "DOC spec-delta: amend \u00a79.2 kind-defined payload description (\"full loop definition: fold ops, edge decls, boundary, search, parse\") to include `lifecycle`, noted as an additive field the Go oracle inherits when \u00a79 is implemented. No frozen-kind change.",
     "est_loc_delta": "+5"
    },
    {
     "path": "libs/lang/tests/test_document.py",
     "role": "Add a lifecycle fixture to ALL_CASES so the parametrized round-trip/idempotence/genesis tests (test_document.py:286-313) cover the new facet.",
     "est_loc_delta": "+15"
    },
    {
     "path": "libs/lang/tests/test_loader.py",
     "role": "Clause-parse tests (well-formed, duplicate-rejected, malformed-rejected).",
     "est_loc_delta": "+30"
    },
    {
     "path": "apps/loops/tests/test_fetch.py",
     "role": "budget()/dispatch hide tests: fold-view omission, Window.limited_by=status + counts, --all defeat, explicit-predicate auto-disable, salience/tier retention, --refs retains inactive walked rows, JSON channel parity.",
     "est_loc_delta": "+80"
    }
   ],
   "planned_changes": [
    {
     "step": "1. Grammar: LifecycleDecl + LoopDef field",
     "description": "ast.py: add frozen LifecycleDecl (mirror EdgeDecl) and LoopDef.lifecycle. loader.py: add the `lifecycle` clause branch in _load_loop_def (mirror the `edge` branch), parsing field name + active/terminal set with duplicate/empty rejection.",
     "files": [
      "libs/lang/src/lang/ast.py",
      "libs/lang/src/lang/loader.py"
     ]
    },
    {
     "step": "2. Declaration document round-trip (SPEC-9 touch point)",
     "description": "document.py: serialize/deserialize the lifecycle facet in _loop_def_to_payload/_loop_def_from_payload using .get() defaults. Do NOT bump DECLARATION_PROTOCOL_VERSION. Add a lifecycle fixture to test_document.py ALL_CASES.",
     "files": [
      "libs/lang/src/lang/document.py",
      "libs/lang/tests/test_document.py"
     ]
    },
    {
     "step": "3. Carry lifecycle down the read path",
     "description": "fold_state.py: add lang-type-free lifecycle field to FoldSection (mirror edge_fields). vertex_reader.py: extract loop_def.lifecycle into it. surface.py: add lifecycle to KindView, populate in project() from the FoldSection.",
     "files": [
      "libs/atoms/src/atoms/fold_state.py",
      "libs/engine/src/engine/vertex_reader.py",
      "apps/loops/src/loops/surface.py"
     ]
    },
    {
     "step": "4. The hide transform (fold-view only, post-project)",
     "description": "surface.py budget(): filter primary (depth==0/level==key) rows whose lifecycle-field value is terminal/not-active per the row's kind's KindView.lifecycle; set Window.limited_by=\"status\", record hidden count; leave walked (--refs, depth>0) rows and the inbound_edges adjacency map untouched. Extend Window + _window_to_dict.",
     "files": [
      "apps/loops/src/loops/surface.py"
     ]
    },
    {
     "step": "5. Defeat + auto-disable wiring",
     "description": "read_args.py: add --all. operation.py: SurfaceSpec.show_all. fold.py view: pass show_all. dispatch.py _project_surface: gate the hide off when spec.show_all, or (per kind) when the kind's lifecycle field is present in spec.where.",
     "files": [
      "apps/loops/src/loops/cli/read_args.py",
      "apps/loops/src/loops/cli/operation.py",
      "apps/loops/src/loops/cli/views/fold.py",
      "apps/loops/src/loops/cli/dispatch.py"
     ]
    },
    {
     "step": "6. Human disclosure",
     "description": "fold.py lens: footer line naming the hidden count and the --all defeat, mirroring the unindexed footer.",
     "files": [
      "apps/loops/src/loops/lenses/fold.py"
     ]
    },
    {
     "step": "7. Validate surface warn (folded-state)",
     "description": "devtools.py _run_validate: add a folded-state pass that reads the store, folds it, and warns on active entities whose edge/ref targets are lifecycle-terminal. validate.py lens: render WARN rows. See open question on whether to build the full --store/--refs surface vs a minimal targeted scan.",
     "files": [
      "apps/loops/src/loops/commands/devtools.py",
      "apps/loops/src/loops/lenses/validate.py"
     ]
    },
    {
     "step": "8. SPEC-9 spec-delta note",
     "description": "Amend loops-go SPEC.md \u00a79.2 kind-defined payload row to name `lifecycle`; note it as an additive field (no frozen-kind change, protocol version unchanged) the Go oracle inherits when \u00a79 is implemented.",
     "files": [
      "/Users/kaygee/Code/loops-go/SPEC.md"
     ]
    },
    {
     "step": "9. Acceptance: real deprecation end-to-end",
     "description": "In a purpose-built test vertex, declare lifecycle on a real kind, emit an entity, transition it to a terminal state via re-emission, then assert: fold-view omission, Window provenance (limited_by=status + count), --all defeat, explicit-predicate auto-disable, salience/tier of visible rows unchanged, inbound edge to the inactive node still counts + stays in --refs, and the validate warn fires when an active entity points at it.",
     "files": [
      "apps/loops/tests/test_fetch.py"
     ]
    }
   ],
   "test_plan": [
    "libs/lang/tests/test_loader.py: lifecycle clause parses (field + active/terminal set); duplicate `lifecycle` in one kind rejected; empty field / empty set rejected; coexists with fold/preview/search/edge.",
    "libs/lang/tests/test_document.py: the ALL_CASES parametrized round-trip (test_roundtrip_via_documents / _via_genesis / _is_idempotent, lines 286-313) passes with a lifecycle fixture \u2014 proves the kind-defined document schema round-trips and stays JSON-safe; a document WITHOUT lifecycle still projects (backward compat via .get()).",
    "apps/loops/tests: budget() hides terminal rows from the fold view; Window.limited_by=='status' + hidden count set; --all restores all rows (limited_by cleared for that read); an explicit `status=<terminal>` predicate auto-disables the hide for that read (per-kind); rows of kinds with no lifecycle decl are untouched.",
    "apps/loops/tests: JSON channel (to_dict(_project_surface(...))) applies the SAME hide as text (invariance) and carries Window.limited_by/counts in _window_to_dict.",
    "apps/loops/tests: EDGE POSITION \u2014 an active entity's inbound edge to a hidden inactive target still contributes to salience (visible rows' salience/tier identical with vs without the inactive entity present) and the inactive node stays present/reachable under --refs (walked depth>0 rows are not dropped by the hide).",
    "apps/loops/tests: validate surface emits an active-targets-inactive WARN on a store where an active entity's edge/ref target is lifecycle-terminal; no warn when the source is itself inactive; exit code stays 0 (warn, not error).",
    "Golden snapshot tests (apps/loops/tests/golden) regenerated for the fold footer disclosure across zoom levels.",
    "Per-package gates: uv run --package lang / atoms / engine / loops pytest; ./dev check at repo root."
   ],
   "acceptance_criteria": [
    "A single kind-level `lifecycle` clause is parseable, round-trips through the _decl.kind-defined declaration document losslessly, and reaches the read path as FoldSection/KindView metadata \u2014 status-aware-kind-budget's `hide` ships as the first tenant of this clause, NOT as a separate sibling clause.",
    "Default `sl read <vertex>` omits terminal/inactive entities of a lifecycle-declaring kind from the FOLD VIEW only; `--facts` (event history) is unchanged; both text and --json see the identical cut.",
    "Window records limited_by=\"status\" with honest shown/total (+ hidden count); the human read shows a footer naming the hidden count and the --all defeat.",
    "`--all` shows everything; an explicit predicate on the lifecycle field auto-disables the hide for that read (per declaring kind).",
    "Inactive entities' inbound edges KEEP counting toward salience (visible rows' salience/tier unchanged by the hide) and inactive nodes STAY reachable in --refs \u2014 the hide is never an edge rewrite; retract remains the distinct correction tombstone.",
    "The `loops validate` surface emits a non-fatal active-targets-inactive warn over folded state (not lang declaration validation).",
    "Acceptance is demonstrated by a REAL deprecation walked end-to-end in a test vertex (declare -> emit -> transition to terminal -> observe all of the above), since the live corpus holds zero inactive entities.",
    "SPEC-9 pricing delivered: the lifecycle facet lands loops-side behind a documented SPEC \u00a79.2 spec-delta note, adds NO frozen _decl.* kind, does NOT bump DECLARATION_PROTOCOL_VERSION, and does not block on the loops-go oracle (which has no \u00a79 code today)."
   ],
   "measures": [
    "SPEC-9 pricing verdict (the required deliverable): (a) frozen _decl.* KIND vocabulary UNCHANGED \u2014 lifecycle is a payload FIELD on the existing _decl.kind-defined document (SPEC \u00a79.2 'full loop definition' row), categorically unlike decl-lens-tombstone-vocab-gap which needs a NEW frozen kind; (b) loops-go must mirror `lifecycle` in its kind-defined document schema WHEN it implements \u00a79 \u2014 today it has zero \u00a79 code, so nothing to mirror against; (c) verdict = LANDS loops-side behind a SPEC.md \u00a79.2 spec-delta note, does NOT block the oracle, does NOT bump protocol version (bumping would make old readers fail-closed on the whole genesis; the \u00a79.2 forward-compat rule 'unknown fields inside a document are ignored' is the sanctioned path for additive fields).",
    "Number of inactive entities hidden in the acceptance test (must be >0 \u2014 a real deprecation, contract-prospective since the live corpus is 0).",
    "Salience-retention delta: visible rows' salience and tier must be byte-identical with vs without the hide applied (target: 0 delta) \u2014 the operational proof of the edge-position invariant.",
    "Test count added and the full ./dev check gate green."
   ],
   "risks": [
    "Scope creep via the validate surface: `loops validate` has no folded-state mode today (devtools.py:20-107 is syntax-only). Building the full --store/--refs/--foreign-refs surface from payload-constraints-in-declarations here could balloon the slice; a minimal targeted edge-scan is safer (see open questions).",
    "Placement error: pushing the hide into project() or the salience/edge corpus (surface.py:152-300, 602, 642, 657) instead of budget() would silently drop inactive nodes' salience contribution and their --refs presence, violating the binding edge-position. The filter MUST stay a post-project budget-stage row-drop scoped to primary (depth==0) rows.",
    "--refs interaction: the hide must skip walked (depth>0) rows or an inactive node explicitly walked-to would vanish, contradicting 'stay in --refs'. Requires an explicit depth guard in the budget filter.",
    "Missing/unknown lifecycle value handling depends on the polarity choice (active-whitelist hides missing values; terminal-blacklist shows them) \u2014 a wrong default could hide legitimately-active entities. Tied to the open polarity question.",
    "Golden churn: the new footer disclosure will move many fold goldens; regenerate deliberately (--update-goldens) and eyeball the diff.",
    "Aggregate/combine vertices: confirm the hide behaves on aggregation reads (per-member KindView must carry lifecycle through the union projection) \u2014 otherwise the hide silently no-ops on the config-level project aggregation."
   ],
   "dependencies": [
    "The shipped internal declaration table / _decl.* document layer (v0.7.0) \u2014 libs/lang/src/lang/document.py + libs/engine/src/engine/declaration.py \u2014 is the SPEC-9 home the lifecycle facet extends; no new table work needed.",
    "The Surface transform pipeline (project -> filter -> select -> count -> budget) in apps/loops/src/loops/cli/dispatch.py:_project_surface and surface.py \u2014 the seam both text and --json go through.",
    "No dependency on graph build-2, digest, or VertexHandle S5 \u2014 this slice is self-contained within the declaration + read path.",
    "loops-go coordination is documentation-only (SPEC.md \u00a79.2 spec-delta); no Go code change is required or possible to block on (\u00a79 unimplemented)."
   ],
   "open_questions": [
    "Clause polarity/vocabulary (genuine design fork): declare the ACTIVE set (whitelist, grammar-domain-neutrality's `lifecycle \"status\" open=\"open,in-progress\"` framing \u2014 hide everything not-active, missing status => hidden) vs the TERMINAL set (blacklist, status-aware-kind-budget's literal `hide status=\"resolved\"` \u2014 hide only named terminals, missing status => shown). The two differ on the missing/unknown-status default and on domain-neutrality. Recommendation: whitelist `active=` for domain-neutrality, but the missing-status default is the arbiter's call.",
    "Validate-surface scope (scope fork): build the full folded-state validate surface (`loops validate --store --refs --foreign-refs`) from payload-constraints-in-declarations now, or bolt a minimal targeted active-targets-inactive edge-scan onto `loops validate` and defer the general surface? The acceptance only needs the warn; recommendation is the minimal scan, but this sets precedent for the larger constraint-validation surface.",
    "DECLARATION_PROTOCOL_VERSION (cross-impl consequence, strong recommendation attached): confirm NO bump (1 stays 1) so old/foreign readers ignore the additive lifecycle field rather than fail-closed on the whole genesis. Flagged because it is a store-canonical, cross-implementation decision even though the \u00a79.2 forward-compat rule makes no-bump the clearly-correct path."
   ]
  },
  {
   "acceptance_criteria": [
    "A fresh, never-sealed store: `sl read <v>` continues to render with NO seal banner by default (byte-identical to pre-slice output) unless the arbiter decision (open question) opts the built-in fold lens into showing one; regardless, `render_context[\"cut\"]` for that read reports `{\"available\": True, \"anchor\": None, \"sealed_to_head\": False, \"tick_total\": 0}`.",
    "A store whose head fact IS the latest tick's `fact_cursor` (fully sealed, e.g. Sol's 694/694 case): `cut[\"available\"] is True`, `cut[\"anchor\"]` is the last tick, `cut[\"sealed_to_head\"] is True`.",
    "A store with a sealed tick followed by newer unsealed facts: `cut[\"available\"] is True`, `cut[\"anchor\"]` names the last tick, `cut[\"sealed_to_head\"] is False`.",
    "An aggregate (combine/discover) vertex: `sl read <agg>` does not raise; `cut == {\"available\": False, \"reason\": \"aggregate vertex \u2014 no single witness cut across members\", ...}`.",
    "A vertex with no store / store not yet created: read still succeeds; `cut[\"available\"] is False` with a stated reason, never a crash and never a guessed 'live unsealed' claim.",
    "`sl read <v> --json` (no --at/--as-of) carries a top-level `cut` key with the same shape as above, for both the Surface gate-pass path and the gate-fail (custom-lens/--lens-override) path.",
    "`sl read <v> --at head` continues to behave exactly as today (existing `cursor` key/behavior/tests untouched) AND additionally carries `cut` derived from the same already-resolved witness position (no extra store I/O).",
    "`sl read <v> --as-of ...` continues to behave exactly as today; `cut` reports `available: False` with a reason naming event-time projection as the cause (no witness-anchored cut in that mode).",
    "A lens (built-in or custom) that declares a `cut` kwarg in its `fold_view` signature receives the populated dict via `call_lens`'s existing signature-filtered kwarg dispatch (`apps/loops/src/loops/lens_resolver.py:481-510`) with zero changes to that dispatch mechanism.",
    "A new shared helper (`cut_line` or equivalent) exists in `apps/loops/src/loops/lenses/_grammar.py` that renders all three states honestly (sealed cut / live head with history / genuinely unavailable) and is exercised by a unit test asserting it never emits any 'live unsealed'-shaped claim when `available is False`.",
    "All existing golden snapshot tests under `apps/loops/tests/golden/` remain green with zero `--update-goldens` diffs (proves the substrate change is additive/inert for the default render path, per whatever the arbiter decides in the open question below)."
   ],
   "current_behavior": "Sol's homelab-audit consumer lens (external, `gruel.network/.loops/audit/lenses/homelab_audit.py:385-464`, NOT part of this repo) demonstrates the exact failure the friction names: `build_doc(state, projection: ProjectionMeta | None = None)` defaults `projection=None`, and when None, line 396 renders `f\\\"Live unsealed fold: {fact_count} current nodes.\\\"` unconditionally \u2014 a positive false claim from missing data. Its own `fold_view` (line 452-464) never passes a `projection` because nothing in loops's read path ever hands one to it.\\n\\nInside strange-loops itself: `apps/loops/src/loops/cli/dispatch.py:226-390` (`dispatch()`) resolves a lens and calls `call_lens(lens_fn, render_data, zoom, width, **extra)` where `extra = dict(op.render_context)` (line 337) plus `vertex_name`/`vertex_path`/`visible`/`chars`/`lines`. For the `read`/`fold` verb, `op.render_context` is built in `apps/loops/src/loops/cli/views/fold.py:733-736` as `{\\\"piped\\\": ..., **({\\\"cursor\\\": cursor_meta} if cursor_meta is not None else {})}`, and `cursor_meta` is ONLY computed (line 608-616) `if args.at or args.as_of`. On an ordinary `sl read <vertex>` (no --at/--as-of \u2014 the dominant path), `render_context` carries no seal/tick provenance key at all \u2014 not `cursor`, not anything else. `apps/loops/src/loops/surface.py`'s `Surface`/`Row`/`Window` dataclasses (lines 62-138) likewise carry zero seal/tick fields. So today, no consumer lens (built-in or custom) can obtain honest seal-cut data for a plain read without reaching around loops and querying the store directly (exactly what Sol's `publish_homelab_audit.py` does, and what the same review flagged as friction #2: \\\"no public sealed-projection/export API exists\\\").\\n\\nThe built-in `fold_view` (`apps/loops/src/loops/lenses/fold.py:379-421,187-223`) is itself NOT buggy today: it only renders a provenance banner (`cursor_mode_line`/`_cursor_lines`) when `cursor is not None`, which happens only for explicit `--at`/`--as-of` reads, and its \\\"no anchor\\\" case (line 217, `\\\"unsealed tail \u2014 no prior tick anchor\\\"`) is honest because it only fires on an ALREADY-resolved witness position. The bug is entirely a substrate-absence problem: nothing analogous exists for the default live-head read, so any consumer who wants that disclosure (as two independent N=2 consumers now have) has no honest data source and reaches for a guess.\\n\\nThe engine already holds everything needed to answer this honestly at read time, reused from the existing --at machinery: `engine.witness.resolve_witness_position(store_path, \\\"head\\\")` (`libs/engine/src/engine/witness.py:275-360`, already invoked for `--at head` via `apps/loops/src/loops/cli/witness_address.py:178-179`) returns a frozen `WitnessPosition` whose `.anchor` (`TickAnchor | None`, witness.py:146-161) is \\\"the last sealed tick at-or-before this position, or None\\\" \u2014 `None` here is a genuine, confidently-known \\\"never sealed\\\" fact, not an unavailability signal. `WitnessPosition.fact_id` compared to `anchor.fact_cursor` tells you, at zero extra query cost, whether the live head IS exactly the sealed cut (fully sealed) or sits beyond it (live tail). `engine.store_reader.StoreReader.tick_total()` (`libs/engine/src/engine/store_reader.py:64`) gives a cheap total-tick count. None of this is wired to the default read path today.\"",
   "dependencies": [
    "None on other 0.9.0 slices \u2014 this is self-contained within apps/loops's CLI read path and reuses existing, already-shipped 0.8.0 engine primitives (resolve_witness_position, TickAnchor, StoreReader.tick_total). Does not touch libs/atoms, libs/lang, or libs/custody."
   ],
   "files": [
    {
     "path": "apps/loops/src/loops/cli/witness_address.py",
     "role": "Add a new function (e.g. `resolve_cut(vertex_path: Path) -> dict`) that wraps `is_aggregate_vertex` + `_resolve_vertex_store_path`-equivalent + `resolve_witness_position(store_path, \"head\")` + `StoreReader.tick_total()` into the JSON-safe `cut` dict, catching every failure mode (no store, aggregate vertex, any WitnessResolutionError, any unexpected exception) into `{\"available\": False, \"reason\": ...}` rather than raising \u2014 a provenance-resolution failure must never abort the read itself. This module already owns exactly this kind of vertex_path\u2192WitnessPosition translation for --at (`is_aggregate_vertex` line 43, `resolve_at_address` line 151), so this is additive, not a new module.",
     "est_loc_delta": "+40/-0"
    },
    {
     "path": "apps/loops/src/loops/cli/views/fold.py",
     "role": "In `run()`, alongside the existing cursor_meta block (lines 605-616), add an independent `cut_meta = _resolve_cut(...)` call (new local helper, or call `witness_address.resolve_cut` directly) that: reuses `at_position.anchor`/`at_position.fact_id` when `--at` is active (no extra I/O); marks `cut` as `available: False` with an event-time reason when `--as-of` is active; otherwise resolves fresh via the new `resolve_cut(vertex_path)`. Thread the result into the `render_context` dict built at lines 733-736 under a new `\"cut\"` key, unconditionally (not gated the way `cursor` is).",
     "est_loc_delta": "+35/-2"
    },
    {
     "path": "apps/loops/src/loops/cli/dispatch.py",
     "role": "Mirror the existing `cursor_meta` JSON-merge pattern for the new `cut` key in both JSON branches: the gate-pass branch (`to_dict(surface)` payload, lines 312-322) and the gate-fail branch (`_render_foldstate_json`'s `extra` dict, lines 182-213, 325-328). No change needed to the plain-text dispatch machinery itself (`extra: dict(op.render_context)` at line 337 already forwards `cut` to `call_lens` for free via the existing signature-filtered kwarg mechanism).",
     "est_loc_delta": "+8/-2"
    },
    {
     "path": "apps/loops/src/loops/lenses/_grammar.py",
     "role": "Add the shared render-honesty helper (e.g. `cut_line(cut: dict, width: int | None) -> Block`) implementing the three-state contract: available+sealed_to_head \u2192 \"sealed\u2026\"; available+anchor+not sealed_to_head \u2192 \"live head \u2014 N sealed tick(s) behind it\u2026\"; available+anchor is None \u2192 \"live head \u2014 unsealed tail, no prior tick anchor\" (reuses the exact phrase already proven honest in `lenses/fold.py:217`); available is False \u2192 \"seal status unavailable\u2026\" (+ reason). This is the module `_grammar.py`'s own CLAUDE.md designates for exactly this: \"Never hand-roll\u2026 a timestamp format, a width=None guard, or a header card in a lens \u2014 import it from here\" \u2014 same discipline applies to this new honesty-ladder primitive so no future lens (built-in or, on next contact, Sol's) re-derives its own None-defaulting logic.",
     "est_loc_delta": "+45/-0"
    },
    {
     "path": "apps/loops/src/loops/lenses/fold.py",
     "role": "OPEN-QUESTION-GATED: if the arbiter opts the built-in fold lens into showing the disclosure by default (see open_questions), add a `cut: dict | None = None` kwarg to `fold_view` (mirroring the existing `cursor` kwarg at line 385) and call the new `cut_line` helper from `_grammar.py`, gated to whatever zoom/visibility the arbiter picks. If the arbiter defers, this file needs NO changes \u2014 the substrate ships inert for the built-in lens and golden tests stay untouched.",
     "est_loc_delta": "+10/-0 or +0/-0"
    },
    {
     "path": "apps/loops/tests/test_fold_view_cursor.py or a new apps/loops/tests/test_fold_cut_provenance.py",
     "role": "New tests using the existing `cursor_vertex` fixture + `_append`/`_append_tick` raw-sqlite helpers (lines 34-70 of test_fold_view_cursor.py) to drive the four cut states end-to-end through `read_view.run(...)` and assert on `payload[\"cut\"]` in `--json` mode, matching the existing `payload[\"cursor\"]` assertion style (test_fold_view_cursor.py:219-232).",
     "est_loc_delta": "+120/-0"
    },
    {
     "path": "apps/loops/tests/test_cli_dispatch.py",
     "role": "Add/extend a case verifying `cut` merges into both JSON branches (gate-pass and gate-fail-with-lens-override) the same way existing cursor-merge behavior is proven there.",
     "est_loc_delta": "+20/-0"
    }
   ],
   "measures": [],
   "open_questions": [
    "Should the BUILT-IN fold lens (`apps/loops/src/loops/lenses/fold.py::fold_view`, the output of plain `sl read <vertex>` with no flags) start VISIBLY rendering the new cut/seal disclosure by default, or should the substrate ship data-only (available to any lens via render_context/JSON, but silent-by-default for the built-in TTY/piped render, exactly as it is today)? This is a genuine fork, not an implementation detail, because: (1) `dispatch.py`'s existing wiring for the sibling `cursor` kwarg unconditionally prepends `cursor_mode_line` above the fold body whenever the kwarg is non-None (fold.py:414-421) \u2014 if `cut` followed the identical pattern and were wired to always render, EVERY `sl read` invocation across the whole CLI would grow a new banner line, which is a highly visible UX change and would require re-baselining the entire golden-snapshot suite (`apps/loops/CLAUDE.md`: \"Golden snapshot tests lock output across all 4 zoom levels\"); (2) leaving it silent-by-default (my recommendation, given `decision:design/drop-truncation-from-human-reads`-style precedent of not eagerly cluttering default reads, and given `horizon`/`orient` already own dedicated venues for seal-state disclosure) means the fix's practical benefit is scoped to consumers who write or declare a lens that opts in \u2014 real for Sol/siftd-shaped consumers (the two N=2 evidence sources), but a plain `sl read project` user gets no visible change. Needs Kyle's call on which failure mode is worse: silent-by-default under-delivers on 'ordinary terminal reads' per the friction's own wording, versus always-on risks feeling noisy/changes output everywhere. A middle option (gate the banner to -vv/FULL zoom only, or only when NOT `sealed_to_head`) is available but is itself a further sub-choice worth deciding alongside this one, not something to default silently."
   ],
   "planned_changes": [
    {
     "step": "1. Engine-adjacent cut resolver",
     "description": "Add `resolve_cut(vertex_path: Path) -> dict` to `apps/loops/src/loops/cli/witness_address.py`. Reuses `is_aggregate_vertex` (line 43) to short-circuit aggregates with `{\"available\": False, \"reason\": \"aggregate vertex \u2014 no single witness cut across members\"}`; otherwise resolves the store path (reuse `loops.commands.resolve._resolve_vertex_store_path`, already imported this way in `cli/views/fold.py:302`) and, when a store exists, calls `engine.witness.resolve_witness_position(store_path, \"head\")` inside a broad try/except (any `WitnessResolutionError` subclass, or any other Exception) that degrades to `available: False` with a reason string rather than propagating \u2014 an informational-provenance failure must never break the read itself. On success builds `{\"available\": True, \"anchor\": {\"name\", \"ts\", \"fact_cursor\"} | None, \"sealed_to_head\": bool, \"tick_total\": int}` where `sealed_to_head = anchor is not None and anchor.fact_cursor == position.fact_id` (zero extra query \u2014 both values are already on the resolved `WitnessPosition`) and `tick_total` comes from `engine.store_reader.StoreReader(store_path).tick_total()` (a cheap COUNT, store_reader.py:64).",
     "files": [
      "apps/loops/src/loops/cli/witness_address.py"
     ]
    },
    {
     "step": "2. Wire into the read view",
     "description": "In `apps/loops/src/loops/cli/views/fold.py::run()`, after the existing --at/--as-of cursor_meta block (lines 605-616), compute `cut_meta` unconditionally: reuse `at_position.anchor`/`at_position.fact_id` (zero extra I/O) when `at_position is not None`; set `{\"available\": False, \"reason\": \"event-time projection has no witness-anchored cut\"}` when `as_of_ts is not None`; otherwise call the new `resolve_cut(vertex_path)`. Add `\"cut\": cut_meta` to the `render_context` dict at lines 733-736, unconditionally (not gated on `is not None` the way `cursor` is \u2014 `cut` should always be present once computed, since 'unavailable' is itself a meaningful, honest value distinct from 'key absent').",
     "files": [
      "apps/loops/src/loops/cli/views/fold.py"
     ]
    },
    {
     "step": "3. Mirror into JSON output",
     "description": "In `apps/loops/src/loops/cli/dispatch.py`, extract `cut_meta = op.render_context.get(\"cut\")` alongside the existing `cursor_meta = op.render_context.get(\"cursor\")` (line 310), and merge it into both JSON branches exactly as `cursor` is merged today: the gate-pass `to_dict(surface)` payload (lines 312-322) and the gate-fail `_render_foldstate_json` call's `extra` dict (lines 325-328).",
     "files": [
      "apps/loops/src/loops/cli/dispatch.py"
     ]
    },
    {
     "step": "4. Shared render-honesty helper",
     "description": "Add `cut_line(cut: dict, width: int | None) -> Block` to `apps/loops/src/loops/lenses/_grammar.py`, implementing exactly the three/four states enumerated in `current_behavior` and `acceptance_criteria` (sealed-to-head / live-with-history / never-sealed / unavailable), reusing the existing honest \"unsealed tail \u2014 no prior tick anchor\" phrase from `lenses/fold.py:217` for the never-sealed case so wording stays consistent across the two related banners. This is the ONE place that owns the never-guess-from-None discipline for cut/seal data \u2014 any lens (built-in, at whatever zoom the open question resolves to, or an external consumer like a rewritten Sol lens) should call this instead of hand-rolling its own default.",
     "files": [
      "apps/loops/src/loops/lenses/_grammar.py"
     ]
    },
    {
     "step": "5. (Contingent on the open question) Wire the built-in fold lens",
     "description": "If Kyle opts the default `fold_view` into visible disclosure: add a `cut: dict | None = None` kwarg (mirroring the existing `cursor` kwarg, fold.py:385) and call `cut_line` from `_grammar.py` at the agreed zoom/condition. If deferred: no change to `lenses/fold.py`, and the substrate still ships fully usable by any lens that declares a `cut` kwarg (built-in or custom) \u2014 `call_lens`'s existing signature-filtered kwarg dispatch (`lens_resolver.py:481-510`) requires zero changes either way.",
     "files": [
      "apps/loops/src/loops/lenses/fold.py"
     ]
    },
    {
     "step": "6. Tests",
     "description": "Extend `apps/loops/tests/test_fold_view_cursor.py` (or a new sibling file) using its existing `cursor_vertex` fixture and raw-sqlite `_append`/`_append_tick` helpers to construct all four cut states (never-sealed empty store, never-sealed store with facts but no ticks, sealed-to-head, live-head-with-history-behind), driving `read_view.run([...])` and asserting `payload[\"cut\"]` via `--json`. Add an aggregate-vertex case (a `combine{}`/`discover{}` declared vertex) asserting `cut[\"available\"] is False` and that the read still succeeds (rc == 0) rather than raising. Add a unit test for `cut_line` covering all four states directly. Add/extend a `test_cli_dispatch.py` case proving the JSON merge on both the gate-pass and gate-fail branches. Run the full golden suite (`uv run --package loops pytest apps/loops/tests/golden`) and confirm zero diffs unless step 5 was taken, in which case regenerate with `--update-goldens` and hand-verify each diff is exactly the new banner line, nothing else.",
     "files": [
      "apps/loops/tests/test_fold_view_cursor.py",
      "apps/loops/tests/test_cli_dispatch.py",
      "apps/loops/tests/golden/"
     ]
    }
   ],
   "risks": [
    "Broad try/except around `resolve_witness_position` in the new `resolve_cut` could accidentally swallow a genuine bug (e.g. a real engine regression) and misreport it as 'seal status unavailable' rather than surfacing it. Mitigation: catch the specific `WitnessResolutionError` family plus a narrowly-scoped fallback, and consider logging/counting the fallback path in tests so an unexpected-exception case is caught in CI rather than silently degrading forever.",
    "If step 5 (built-in fold_view rendering) is taken without care, it changes output for every `sl read` across the whole test suite and any downstream consumer (hooks, session-orient scripts, other agents' tooling) that scrapes plain-text output \u2014 `apps/loops/CLAUDE.md`'s own history flags this exact failure mode (the orient hook's shell-grep breakage documented in `commands/orient.py`'s module docstring). Any golden-updating pass must be reviewed line-by-line, not bulk-approved.",
    "`sealed_to_head` conflates 'this exact fact is the tick's fact_cursor' with `store verify`'s broader 'N facts covered by the union of all tick windows' metric (`commands/store.py:295-299`) \u2014 these usually coincide for a store that seals sequentially via a `when=seal` boundary but are NOT proven identical in every topology (e.g. a boundary that doesn't fire on every fact, or multiple interleaved boundary series). The banner text must say 'sealed at tick X' (a true, narrow claim) rather than implying store-wide seal coverage parity with `store verify`'s number \u2014 misphrasing here would reintroduce a milder version of the same honesty bug this slice fixes.",
    "The new `cut` JSON key is additive and none of the existing JSON tests do full-dict equality (confirmed: `test_fold_view_cursor.py` asserts via `payload[\"cursor\"][...]` key lookups, and no JSON goldens exist under `apps/loops/tests/golden/goldens/`), but any future JSON consumer doing strict schema validation elsewhere (outside this repo, e.g. Sol's publisher) would see a new key appear \u2014 purely additive, non-breaking by construction, but worth a one-line mention wherever the JSON contract is documented if such a doc exists."
   ],
   "slice": "SLICE 6 \u2014 honest seal provenance (friction:seal-provenance-not-passed-to-lenses)",
   "test_plan": [
    "uv run --package loops pytest apps/loops/tests/test_fold_view_cursor.py apps/loops/tests/test_fold_cursor_render.py apps/loops/tests/test_cursor_capstone_findings.py apps/loops/tests/test_cursor_review_findings.py apps/loops/tests/test_witness_address_anchor.py -x  (prove zero regression to existing --at/--as-of behavior)",
    "uv run --package loops pytest apps/loops/tests/test_fold_cut_provenance.py -x  (new: all four cut states via --json, end-to-end through read_view.run)",
    "uv run --package loops pytest apps/loops/tests/test_cli_dispatch.py -x  (JSON-merge on both gate-pass/gate-fail branches)",
    "uv run --package loops pytest apps/loops/tests/golden -x  (byte-identical unless step 5 taken; if taken, --update-goldens then hand-diff every changed golden)",
    "uv run --package loops pytest apps/loops/tests  (full app suite, catch any signature-dispatch surprise in call_lens/call_lens_fetch)",
    "uv run --package engine pytest libs/engine/tests/test_witness_position.py  (confirm no engine-side change was needed \u2014 resolve_cut is CLI-layer composition only)",
    "Manual, per CLAUDE.md's install discipline: `uv tool install . -e` then drive `sl read project --json | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"cut\"])'` against the real project.vertex store (which per MEMORY.md was sealed at v0.8.0/ba802ce) to confirm a real, non-synthetic sealed_to_head or live-head-with-history reading; also exercise against a scratch aggregate vertex to confirm the aggregate refusal path degrades gracefully rather than raising."
   ],
   "thesis": "Ordinary `sl read` (and any consumer-authored lens) currently receives ZERO seal/tick provenance for the default live-head read \u2014 the render_context channel that carries this kind of disclosure (\\\"cursor\\\") exists and is honest, but is wired ONLY for explicit --at/--as-of reads. Two independent consumers (homelab-audit 2026-07-12, siftd 2026-07-18) hit this absence and, lacking any real data, guessed wrong (\\\"Live unsealed fold\\\" rendered from a None default, contradicting `store verify`'s 694/694 sealed). This slice threads the SAME witness-position machinery already used for `--at head` into every default read (as a new, additive `cut` render_context/JSON key, decoupled from the --at/--as-of refusal-gating machinery so it can never regress custom-lens compatibility), and ships one shared, honest three-state rendering helper so no lens \u2014 built-in or external \u2014 has to hand-roll (and get wrong) its own None-defaulting logic again.</thesis>\n"
  }
 ],
 "sequence": {
  "order": [
   "Slice 1 \u2014 typed Address(kind,key) dataclass (converge emit/read/match)",
   "Slice 2 \u2014 FTS read-purity (sl store reindex sole writer, read-only vertex_search)",
   "Slice 3 \u2014 graph build-2 (predicate-aware graph projection)",
   "Slice 6 \u2014 honest seal provenance (cut render_context/JSON key)",
   "Slice 4 \u2014 canonical review projection + cursor/decl-generation disclosure",
   "Slice 5 \u2014 declarative entity lifecycle (kind-level lifecycle clause + fold-view hide)"
  ],
  "rationale": "Two hard chains drive the spine: S1 before S3 (typed-address reshapes the corpus the graph walk reads \u2014 S3 must consume the landed atoms.Address, not re-derive splits) and S2 before S4 (S4's review projection assumes S2's read-only-store posture is landed). Both foundational substrate-reshapers (S1 atoms/engine match-path + read-lift; S2 engine FTS + surface search/Window) go first and adjacent so the four read-path slices (S3/S4/S5/S6) all build on a stable substrate, minimizing rework \u2014 this dominates raw collision-counting. After foundation, S3 slots next because it shares zero *edited* files with S2 (S3 only reads surface.py; S2 edits it) \u2014 the cleanest transition. S6 (seal provenance) then S4 (review + head-cursor) are placed adjacent because they overlap on head-witness-position resolution in fold.py::run() \u2014 putting them consecutive keeps that coordination inside one fresh-context boundary. S5 (lifecycle) is last: it is the largest, most cross-cutting slice (touches lang/atoms/engine + the entire read path + dispatch + validate) and is self-contained w.r.t. the others, so it builds on everyone's final state; its lang/ edits are isolated (no other slice touches lang/). The two unavoidable heavy consecutive same-file pairs are (S1,S2) and (S4,S5) \u2014 both edit surface.py/vertex_reader.py but in DISJOINT function regions (match-path vs search/Window; to_review vs budget/KindView), so the collisions are low-risk re-checks rather than true overlap. Consecutive same-file collision total \u22489 (vs 11 for naive 1-2-3-4-5-6); no ordering eliminates the surface.py/cli-views-fold.py clustering because S3/S4/S5/S6 all thread new logic through the same read-path files.",
  "conflicts": [
   {
    "files": [
     "apps/loops/src/loops/surface.py"
    ],
    "slices": [
     "Slice 1 (match-path: _inbound_count gains kind, _address_matches_key deleted \u2192 Address exact-match)",
     "Slice 2 (search() + Window staleness field + _indexed_kinds routing)",
     "Slice 4 (new to_review encoder beside to_dict; FoldItem-field whitelist)",
     "Slice 5 (KindView.lifecycle, budget() hide, Window limited_by='status' + hidden-count, _window_to_dict)"
    ],
    "note": "4-way, mostly disjoint functions. Later touchers must re-check: (1) Window dataclass shape \u2014 S2 adds a staleness field, S5 adds a hidden-count field + 'status' to limited_by; S5 (last) MUST build on S2's shipped Window, and both edit _window_to_dict (JSON contract). (2) S4's to_review whitelist must keep EXCLUDING every read-time derivation, including any lifecycle-derived display field S5 introduces \u2014 verify no leak. (3) S1's match-path rewrite (inbound_edges/Row.address semantics) must be final before S3/S4/S5 read it. (4) S1 changes _inbound_count's signature (adds kind) touching call sites 581/601/614/634 \u2014 confirm none reverted."
   },
   {
    "files": [
     "apps/loops/src/loops/cli/views/fold.py"
    ],
    "slices": [
     "Slice 3 (thread args.edge into call_lens_fetch branch only)",
     "Slice 6 (cut_meta into render_context, unconditional; head-witness resolve)",
     "Slice 4 (--review short-circuit branch + extend cursor_meta to HEAD reads)",
     "Slice 5 (thread show_all into SurfaceSpec)"
    ],
    "note": "4-way on run() + the render_context dict (lines 733-736) + _build_fold_fetch. Each later slice adds a new meta/branch and must NOT clobber earlier keys: S6 adds 'cut', S4 adds/extends 'cursor' for head, plus S4's --review branch must sit correctly relative to the existing --why/--diff short-circuits and S6's cut computation. CRITICAL: S4's head-cursor resolution and S6's cut both call resolve_witness_position(store,'head') for a default read \u2014 the S4 agent (later) must reuse/factor with S6's resolve_cut, not add a second independent head-resolve."
   },
   {
    "files": [
     "libs/engine/src/engine/vertex_reader.py"
    ],
    "slices": [
     "Slice 1 (_lift_edges 1245-1267 + delete _normalize_edge_address 1229)",
     "Slice 2 (remove _ensure_fts write from vertex_search 1651-1726; add vertex_reindex + vertex_search_coverage; delete _ensure_fts)",
     "Slice 5 (extract loop_def.lifecycle into FoldSection build, 934-961)"
    ],
    "note": "3-way but disjoint regions (edge-lift ~1229-1267 vs FTS ~1585-1726 vs FoldSection build ~934-961). S2 must confirm S1's deletion of _normalize_edge_address didn't shift anchors it relies on. S5 (last) builds on both \u2014 its lifecycle extraction sits beside the existing edge_specs/preview_fields extraction; confirm final state of that block."
   },
   {
    "files": [
     "apps/loops/src/loops/lenses/fold.py"
    ],
    "slices": [
     "Slice 2 (_render_search stale-index footer + MINIMAL line, 559-618)",
     "Slice 6 (OPTIONAL cut kwarg on fold_view + cut_line \u2014 arbiter-gated)",
     "Slice 5 (fold footer hidden-count line, EXPLICITLY mirroring the window.unindexed footer at 610-616)"
    ],
    "note": "S5's hidden-count footer mirrors the SAME footer region (610-616) that S2 rewrites for staleness \u2014 S5 (last) MUST build on S2's post-stale footer code, not baseline. S6's cut kwarg (if the fork is taken) sits alongside the existing cursor kwarg at fold_view."
   },
   {
    "files": [
     "apps/loops/src/loops/cli/dispatch.py"
    ],
    "slices": [
     "Slice 6 (merge 'cut' into both JSON branches \u2014 gate-pass to_dict 312-322 + gate-fail _render_foldstate_json 325-328 \u2014 mirroring cursor_meta)",
     "Slice 5 (gate the budget() lifecycle hide in _project_surface, line 178)"
    ],
    "note": "S5 (later) must leave S6's 'cut' JSON-merge in both branches intact; S5's budget-gate reads spec.show_all/spec.where and must not disturb the cursor/cut merge blocks (310-328)."
   },
   {
    "files": [
     "libs/engine/src/engine/__init__.py"
    ],
    "slices": [
     "Slice 2 (add vertex_reindex + vertex_search_coverage to __all__ + lazy map)",
     "Slice 4 (add declaration_generation + fact_signatures)"
    ],
    "note": "S4 (later) appends to __all__ (line 20+) and the lazy-import map \u2014 do not overwrite S2's additions; mirror the existing vertex_search entry pattern (line 90/218)."
   },
   {
    "files": [
     "apps/loops/src/loops/cli/read_args.py"
    ],
    "slices": [
     "Slice 3 (--edge, with predicate completer)",
     "Slice 4 (--review store_true)",
     "Slice 5 (--all store_true)"
    ],
    "note": "Purely additive \u2014 none of --all/--edge/--review exist today (verified). Each adds a distinct add_argument; low overlap. Later touchers confirm completer wiring and that the fold parser sees all three flags."
   },
   {
    "files": [
     "apps/loops/tests/test_surface.py"
    ],
    "slices": [
     "Slice 1 (anti-aliasing regression: decision:X vs thread:X)",
     "Slice 2 (reindex the _search_vertex fixture 493-521 + unreindexed-fallback test)"
    ],
    "note": "S2 (later) inserts vertex_reindex into the _search_vertex fixture without disturbing S1's added exact-match cases; confirm the four downstream FTS tests genuinely exercise FTS post-fix, not the substring fallback."
   },
   {
    "files": [
     "apps/loops/tests/golden/ (tests.golden.test_grammar_parity/)"
    ],
    "slices": [
     "Slice 3 (graph snapshots [*-graph] \u2014 definite: predicate labels + dangling/filter-excluded breakdown)",
     "Slice 6 (fold header \u2014 CONDITIONAL on default-read cut-disclosure fork)",
     "Slice 4 (fold snapshots \u2014 CONDITIONAL on default-read head-cursor fork)",
     "Slice 5 (fold footer \u2014 definite: hidden-count line)"
    ],
    "note": "Shared golden tree, serialized. Graph goldens are S3-isolated. Fold goldens are touched by S5 (definite) and conditionally by S4/S6 \u2014 the last fold-golden toucher (S5) regenerates over any S6/S4 conditional header additions and MUST hand-verify every diff line is intended, never bulk --update-goldens. Resolve the S4/S6 'visible on default read?' forks before any of these regenerate so the fold baseline moves once."
   }
  ],
  "warnings": [
   "CROSS-REPO EDIT (S5): Slice 5 lists /Users/kaygee/Code/loops-go/SPEC.md as an edited file (+5 LOC \u00a79.2 spec-delta), and Slice 4 READS the same file (\u00a710 boundary check). Verified: this path exists but lives OUTSIDE the loops repo AND outside the additional working dir (gruel.network). It cannot ride the loops feature branch \u2014 it is a separate loops-go commit. Flag to the arbiter/orchestrator: confirm the S5 agent has write access to loops-go, or split the SPEC.md note into a follow-up loops-go change; do not let S5 block on an un-writable path.",
   "S4\u2194S6 HEAD-DISCLOSURE OVERLAP: both slices resolve the head witness position for a plain (no --at/--as-of) read. S6 adds resolve_cut \u2192 resolve_witness_position(store,'head') + tick_total producing a new 'cut' JSON key; S4 step 6 adds a head cursor_meta via resolve_at_address(store,'head') extending the existing 'cursor' key. They are distinct keys with related semantics. In build order S6 precedes S4 (adjacent) \u2014 the S4 agent must REUSE/factor with S6's head-resolution rather than add a second independent head-position resolve to fold.py::run(). Risk of two parallel head-resolve paths if built blind.",
   "UNRESOLVED ARBITER FORKS BLOCK DETERMINISTIC IMPLEMENTATION \u2014 several are contract/JSON-shaping, not cosmetic, and are NOT decided in the briefs: S2 (Window.stale new field vs reuse unindexed \u2014 changes --json shape); S3 (filter-semantics sub-scope-vs-global for \u2190N/chains; outbound-keyless taxonomy = the dict-key set; --edge-without-graph refuse posture); S4 (declaration-generation mechanism = a NEW disclosed contract field; head-cursor default-read scope); S5 (clause polarity active-whitelist vs terminal-blacklist = the missing-status default; validate-surface scope; DECLARATION_PROTOCOL_VERSION no-bump); S6 (built-in fold lens visible-by-default vs data-only); S1 (Edge.address STR-STAYS vs typed-field). A fresh agent cannot pick these deterministically. Resolve each affected slice's contract fork BEFORE that slice starts.",
   "S1 Edge.address FORK FEEDS S3: Slice 3's node-membership + (e)-classification are designed to route through ONE helper that swaps to Address equality 'in one place' once S1 lands. If S1 resolves STR-STAYS (recommended), S3's helper parses the canonical 'kind:key' string via Address.parse \u2014 works. If the arbiter picks typed-field, S3's helper shape differs. S1's resolution MUST be communicated to the S3 agent before S3 begins, else rework.",
   "WINDOW DATACLASS DOUBLE-EXTEND (S2 + S5): both extend surface.py Window and _window_to_dict AND both mutate the limited_by vocabulary (S2 adds a staleness axis; S5 adds 'status'). S2 (earlier) locks the Window shape S5 inherits. The arbiter's Window-field decision (S2's open_question) must land before S2 is implemented, and S5 must conform to whatever S2 actually shipped \u2014 do not let S5 re-open the field-naming choice.",
   "GOLDEN BASELINE SHOULD MOVE ONCE, NOT THRICE: fold goldens are churned by S5 (definite footer) and conditionally by S4 and S6 \u2014 both S4/S6 conditions are the SAME class of decision (does a plain `sl read` grow a banner line?). Decide the S4 head-disclosure fork and the S6 built-in-lens fork TOGETHER and before any of S3/S4/S5/S6 regenerate goldens, so the most-used command's output baseline shifts once. Graph goldens (S3) are isolated and unaffected.",
   "MINOR UNDER-SPECIFICATION FOR FRESH AGENTS: (a) S5 names apps/loops/tests/test_fetch.py as its acceptance-test home, but its SOURCE changes are in surface.py::budget + cli/dispatch.py::_project_surface, NOT commands/fetch.py \u2014 an agent hunting for fetch.py source edits finds none. (b) S6 leaves its test file ambiguous ('test_fold_view_cursor.py OR a new test_fold_cut_provenance.py') \u2014 pick one up front. (c) S3's cli/completers.py + fish/bash glue are marked OPTIONAL/deferrable \u2014 confirm whether completion is in-scope for 0.9.0 before the S3 agent invents it.",
   "SAME DEFECT CLASS IN TWO PLACES (S1 + S3, complementary not duplicate): S1 fixes suffix-only cross-kind aliasing in surface.py match-path; S3's graph node-membership/(e)-classification is the SAME root defect (kind-blind suffix match) in commands/fetch.py. Since S3 is built after S1, verify S3 consumes the landed atoms.Address (S3's mandated single membership helper references Address equality) rather than re-deriving its own split/suffix logic \u2014 otherwise the fix lands in one consumer and silently persists in the other."
  ]
 }
}