// Disk usage monitor
// Parses df -h output to track capacity per mount point

source "df -h"
every "5s"
kind "disk"
observer "disk-monitor"

parse {
  skip "^Filesystem"
  split
  pick 4 5 { names "pct" "mount" }
  transform "pct" { strip "%" coerce "int" }
}
