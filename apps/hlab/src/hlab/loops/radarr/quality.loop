// Radarr quality definitions
// Variables: ${host}, ${apikey}
// Radarr returns a JSON array; format: json wraps it as {"_json": [...]}.

source #"curl -sf "http://${host}/api/v3/qualitydefinition?apikey=${apikey}""#
kind "quality"
observer "hlab"
format "json"

parse {
  explode path="_json"
}
