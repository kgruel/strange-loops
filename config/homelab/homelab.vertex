// Homelab — per-VM agent pattern
// Designed to run on each VM, replacing traditional exporters
// Collects local metrics and emits ticks upstream

name "homelab"
store "./data/homelab.db"

// TODO: sources — per-VM metric collection loops
// TODO: design the agent pattern (local loops → central vertex)

discover "./**/*.vertex"
