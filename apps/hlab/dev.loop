# Dev stack container status
# SSH to dev VM, get docker compose ps output

source: ssh deploy@192.168.1.41 "cd /opt/dev && docker compose ps --format json"
kind: dev
observer: hlab
format: ndjson

parse:
  select Name, Service, State, Health, Status, ExitCode, Image, Ports, Project, CreatedAt, RunningFor
