No R7 findings.

**CONVERGED.** The winner is resolved once before replay, and both emission orders produce string-only, content-invariant attribution with no native fields or smuggled priors. Native-only and interleaved-supersession evasions also pass. A true `0`/`"0"`/`False` three-way projected collision is not constructible: `False` projects to `"False"` and aliases `0` as a Python dictionary key.

Verification:

- Targeted review: 20 passed
- Full suite: 2521 passed, 1 xfailed
- `./dev check`: 59 passed
- `git diff --check`: clean

Residual risk: the accepted duplicate-address ambiguity remains—native `0` and string `"0"` can surface as separate rows sharing `kind/0`, while `--why` deterministically explains the string identity. Losing facts remain visible in chronology with empty `changed` sets. This is consistent with the held design question at `thread:fold-key-identity-native-vs-string` and does not block closure.
